# Прогон check_C37 (сессия 9, 13.09.2026)

Цель: независимая проверка C37 (`docs/proofs/C37_toric_correlation.md`) —
теоремы A (тождество) вместе с её промежуточными леммами 1–3, теоремы B
(оценка энергии и случай равенства), следствия C и замечаний 3–5.

Команда: `python3 checks/check_C37.py --amax 6 --bmax 7 --rmax 12 --trials 20`
(seed 37). Версия чекера — в заголовке файла; ядро ходов не используется.
Чекер не импортирует `experiments/toric_correlation.py`: `sig`, `S`, `E`,
линия разреза и подсчёт инверсий реализованы заново по определению H13-I.

Покрытие:

- часть A: все pi и все n^2 разрезов при 2 <= n <= 6 (в каждом разрезе
  проверены лемма 1 для всех i, лемма 2, постоянство Phi и теорема A),
  случайные pi при 7 <= n <= 12 (по 20 на n);
- часть B: все pi при 2 <= n <= 7; случайные при 8 <= n <= 12; отражения
  при 4 <= n <= 40 и n = 60, 100, 200;
- часть C: все pi при 4 <= n <= 7, `I(pi)` сверен и по формуле, и перебором;
- часть D: эквивалентная форма H13-I — все pi при 4 <= n <= 7 (5904 pi);
  замкнутая формула `S = C((q+s-h) mod n)` для всех отражений при 4 <= n <= 20.

Время: 6.3 с. Результат: PASS (0 расхождений во всех частях).

Артефакты: `report.json`, `report.md`.

Вывод: C37 подтверждена независимой реализацией; статусы PROVED (теоремы A, B,
следствие C) не опираются на конечный перебор — перебор лишь контролирует
выкладки.
