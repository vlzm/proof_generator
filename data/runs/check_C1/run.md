# check_C1 — прогон 13.09.2026 (сессия 6)

Цель: независимый аудит C1 (`docs/incoming/lrx_2n3_proof_checked.md`,
`D_n <= floor(A_n)`, следствие `D_n <= B_n + floor(4n/3) - 3`). Полный
разбор переходов — `docs/notes/C1_audit.md`.

Команда: `python3 checks/check_C1.py --exhaustive 9 --scalar 100000 --sharp 25`

Версии: check_C1-1.0, ядро oracle-1.0, конструкция strict_upper-1.0.
Время: 240.5 с. Память — не измерялась отдельно (потоковый перебор).

Покрытие и результат:

1. Неравенство (5) `3nS(C) >= 5(F(C)+k)`: 6 590 710 экземпляров цикла
   (все нетривиальные циклы f_c по всем pi, всем c, 4 <= n <= 9) —
   0 нарушений. Точные (равенство) случаи: (n=6,k=6,M=3,E=0),
   (n=8,k=8,M=3,E=0), (n=9,k=8,M=3,E=0) — последние два совпадают с
   семейством §6 источника (C_r при r=2, встроенным в f_c для некоторых
   pi, c при этих n).
2. Цепочка округления (точная арифметика, без float): `floor(A_n)`
   совпадает с целочисленной формулой источника (`stated_bound`) на
   4 <= n <= 100000, 0 расхождений; вторая (более слабая) формула
   `B_n+floor(4n/3)-3` — верхняя оценка для `floor(A_n)` на всём диапазоне,
   0 нарушений (текст указывает более узкие пороги для своего
   аналитического аргумента, но численно вторая формула достаточна везде).
3. Семейство §6 (`C_r`, n=4r): r = 2..25 (n = 8..100) — точное равенство
   в (5) на каждом r, все статистики (k=8,t=4,E=0,F=3n-8,M=3,S=5) совпали.
4. Кросс-исполнение: 3 628 776 слов, построенных эталонным кодом источника
   (`docs/incoming/lrx_2n3_check.py`), повторно исполнены независимым
   движком `oracle/moves.py` (не собственным `apply_word` источника) —
   все сортируют pi, все укладываются в целочисленную границу источника —
   0 отказов.

Итог: **check_C1: PASS**. Реестр — C1 переведена в PROVED
(`docs/notes/C1_audit.md`, CLAIMS.md).

Следующий вопрос: не задан. Артефакт — `data/runs/check_C1/report.json`.
