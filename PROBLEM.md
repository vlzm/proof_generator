# PROBLEM.md — LRX: диаметр графа Кэли

Постановка, модель, конвенции, установленные факты и оценки с явным статусом.
Стабильный файл: меняется при исправлении ошибки, появлении нового результата
или уточнении его статуса. Правила работы — AGENTS.md, гипотезы и план — PLAN.md,
реестр утверждений — CLAIMS.md.

Редакция: 13 сентября 2026; учтён `LRX_STRICT_PROOF_RU-1.md` (N1); §5.5 — C32.

---

## 1. Задача

Дан массив длины n. Три хода, каждый стоит один шаг:

```text
L : (x0, x1, ..., x(n-1)) -> (x1, ..., x(n-1), x0)     циклический сдвиг влево
R : обратный к L                                        циклический сдвиг вправо
X : (x0, x1, x2, ...)     -> (x1, x0, x2, ...)          обмен двух первых
```

Граф Кэли: вершины — все n! перестановок, ребро — один ход. Граф
неориентированный (набор ходов замкнут относительно обратных) и связный
(сопряжения X степенями L дают все соседние транспозиции, а они порождают S_n).

Обозначения:

- `id_n = (0,1,...,n-1)` — тождественная перестановка.
- `dist(p,q)` — длина кратчайшего пути между вершинами.
- `d(p) = dist(id_n, p)` — минимальное число ходов, сортирующих p.
- `D_n = max_p d(p)` — диаметр. Граф вершинно-транзитивен, поэтому
  расстояний от id_n достаточно.
- `B_n = n(n-1)/2` — целевая формула диаметра.
- `sigma_n[i] = (1-i) mod n` — предполагаемый единственный самый далёкий элемент.
- `rev_n = (n-1, n-2, ..., 0)` — обычный разворот.

Главная цель: доказать `D_n = B_n` при всех n >= 4.
При n = 3 диаметр равен 2, а B_3 = 3; поэтому область — n >= 4.

Отдельные цели, не следующие из равенства для диаметра: единственность
sigma_n на расстоянии D_n; равенство `d(rev_n) = D_n - 2`.
Формула точного d(p) для всех p — вспомогательное направление
с ограниченным бюджетом, а не предпосылка решения.

---

## 2. Модель «окружность + голова»

Пример. Слово XRRXRR ведёт из id_4 в sigma_4 = (1,0,3,2):

```text
(0,1,2,3) -X-> (1,0,2,3) -R-> (3,1,0,2) -R-> (2,3,1,0) -X-> (3,2,1,0) -R-> (0,3,2,1) -R-> (1,0,3,2)
```

Определение. Голова — пара соседних позиций окружности, которую видит X.
Массив — это чтение окружности, начиная с головы, поэтому в массиве голова
всегда стоит на индексах (0,1), а на окружности она двигается.

То же слово в картинке. Элементы лежат по окружности, скобка — голова:

```text
[0 1] 2 3      старт: окружность 0-1-2-3, голова на паре (0,1)
[1 0] 2 3      X   : обмен под головой; окружность стала 1-0-2-3
[2 3] 1 0      RR  : голова уехала на два шага; окружность та же
[3 2] 1 0      X   : окружность стала 1-0-3-2 — та же, обойдённая в обратную сторону
[1 0] 3 2      RR  : голова вернулась; это sigma_4
```

Общий принцип:

- Состояние = (порядок элементов на окружности, положение головы).
- Позиции окружности удобно считать неподвижными: L двигает голову на одну
  позицию вперёд (к большим номерам), R — назад; порядок элементов не трогают.
- X меняет только содержимое двух позиций под головой.
- Порядок на окружности с точностью до поворота — класс окружности.
  Классов (n-1)!, положений головы n, состояний n! — всё сходится.

Определения. N_X — число обменов в слове; N_rot — число вращений
(ходов L и R), он же пробег головы.

Стоимость слова = N_X + N_rot. Если обмены сделаны в позициях h_1, ..., h_k
окружности, то в кратчайшем слове голова между соседними обменами идёт
по короткой дуге, и

```text
d(p) = min по последовательностям позиций обмена h_1..h_k, сортирующим p,
       [ k + delta(h_0,h_1) + delta(h_1,h_2) + ... + delta(h_k,h_(k+1)) ]

h_0 — голова в начале, h_(k+1) — где голова обязана оказаться в конце.
```

Следствия, верные всегда:

- В кратчайшем слове нет XX (сократилось бы), значит соседние обмены —
  в разных позициях и `N_rot >= N_X - 1`.
- Внутри одного класса окружности d меняется не больше чем на floor(n/2):
  одно состояние переводится в другое одними вращениями головы.
- rev_n и sigma_n лежат в одном классе (окружность, обойдённая в обратную
  сторону), и `apply_word(sigma_n, "LL") = rev_n`. Поэтому
  `d(rev_n) >= d(sigma_n) - 2` при всех n; утверждение C6 — про равенство.

Задача о диаметре распадается на две: «самый далёкий класс окружности»
и «поправка на положение головы». Оба фронта живут в этой модели:
верхняя оценка — маршрут головы плюс обмены; нижняя — «при любой стратегии
обменов не меньше A и пробега не меньше B».

---

## 3. Конвенции

- 0-индексация везде. Перестановка p — кортеж; позиция i содержит p[i].
- Слова исполняются слева направо: `XL` — сначала X, затем L.
  `pL`, `pR`, `pX` — применение хода к p. Операторную запись вида `L^2 p`
  не используем; пиши `apply_word(p, "LL")`.
- Композиция функций: `(a o b)[i] = a[b[i]]`. Если `l[i] = (i+1) mod n`,
  то `apply_move(p, "L") = p o l`. При переносе внешних формул отдельно
  сверяй сторону действия (слева или справа) и порядок произведения.
- `delta_n(a,b) = min((b-a) mod n, (a-b) mod n)` — расстояние по окружности позиций.
- «Цикл перестановки» — цикл в её разложении; «окружность» — позиции Z_n.
- Перестановки записываются кортежами в 0-индексации; при цитировании другой
  нотации рядом даётся перевод в проектную.
- Язык документации и общения — русский, простой, без неразвёрнутого жаргона;
  код, идентификаторы и комментарии в коде — английский.

Обязательный проверочный пример — при любом сомнении в порядке действий:

```text
(2,0,1) --X--> (0,2,1) --L--> (2,1,0)
apply_word((2,0,1), "XL") == (2,1,0)
```

---

## 4. Известные данные

### 4.1. Точные значения при 4 <= n <= 12

Данные 4..8 — из исходного задания; 12.09.2026 в репозитории построены
и сертифицированы таблицы для 4 <= n <= 12 (`data/tables/`, сертификат
`oracle/verify_distances.py`; ускоренный `exact/bfs_fast.c` побайтово
совпадает с эталонным Python BFS на 4 <= n <= 11).

| n | 4 | 5 | 6 | 7 | 8 | 9 | 10 | 11 | 12 |
|---|---|---|---|---|---|---|---|---|---|
| D_n | 6 | 10 | 15 | 21 | 28 | 36 | 45 | 55 | 66 |
| Вершин на расстоянии D_n | 1 | 1 | 1 | 1 | 1 | 1 | 1 | 1 | 1 |
| Вершин на расстоянии D_n - 1 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 3 |
| d(rev_n) | 4 | 8 | 13 | 19 | 26 | 34 | 43 | 53 | 64 |

Единственная самая далёкая вершина — sigma_n:

```text
n=4: (1,0,3,2)   n=5: (1,0,4,3,2)   n=6: (1,0,5,4,3,2)   n=7: (1,0,6,5,4,3,2)   n=8: (1,0,7,6,5,4,3,2)
и так далее до n=12 (проверено сертифицированными таблицами)
```

sigma_n — отражение окружности позиций, переводящее ребро {0,1} в себя.
Это инволюция: при чётном n неподвижных точек нет, при нечётном — одна;
остальные циклы имеют длину 2. Слой D_n - 1 при 4 <= n <= 8 — ровно три
соседа sigma_n.

Равенство D_n = B_n, единственность sigma_n и d(rev_n) = D_n - 2 — разные
утверждения; за пределами 4..8 каждое требует собственного обоснования.

### 4.2. Строение кратчайших слов до sigma_n

Определение. Геодезическая — кратчайшее слово от id_n до данной вершины.

Наблюдение при ревью (BFS, 4 <= n <= 8; в реестре — C13):

```text
n    D_n   N_X    N_rot    floor((n-1)^2/4)   floor(n^2/4)   число геодезических
4     6     2*      4*            2                4                 16
5    10     4       6             4                6                 20
6    15     6       9             6                9                 60
7    21     9      12             9               12                 84
8    28    12      16            12               16                448
                                          * при n=4 есть и вариант N_X=3, N_rot=3
```

При 5 <= n <= 8 все геодезические до sigma_n имеют одно и то же расщепление:
`N_X = floor((n-1)^2/4)`, `N_rot = floor(n^2/4)`.

Тождество (C12, доказательство — разбор чётности):

```text
B_n = floor(n^2/4) + floor((n-1)^2/4)
n = 2m:    m^2 + (m^2 - m) = m(2m - 1) = n(n-1)/2
n = 2m+1:  (m^2 + m) + m^2 = m(2m + 1) = n(n-1)/2
```

Отсюда `N_rot = N_X + floor(n/2)`: сверх обязательных `N_X - 1` шагов голова
делает ещё `floor(n/2) + 1`.

Примеры геодезических, по одной на n:

```text
n=5: XRXRRXRRXR
n=6: XRXLXLLXLXRXRRR
n=7: XLXRXRXLXLXLLXLXRXRRR
n=8: XRXLXLXRXRXRRXRXRXLXLXRXRRRR
```

Обмен — одно-два вращения — обмен — ...; в этих примерах между обменами не
больше двух вращений, в конце — хвост из вращений. Это материал для
конструкции и мишень для нижней оценки; общий закон отсюда не следует.

### 4.3. Преобразования, сохраняющие расстояние до id_n

```text
d(p) = d(p^{-1})
d(p) = d(sigma_n o p o sigma_n)
```

Первое: перевернуть слово и заменить каждый ход обратным — получится слово
для p^{-1}. Но `p -> p^{-1}` не сохраняет рёбра. Пример при n = 4:

```text
p = (1,0,2,3),  pL = (0,2,3,1),  p^{-1} = (1,0,2,3),  (pL)^{-1} = (0,3,1,2)
```

Последние две вершины не соединены ни одним ходом.

Второе — автоморфизм графа: сопряжение sigma_n переводит L в R, R в L, X в X
и сохраняет id_n. Других отображений вида `p -> g o p o g^{-1}`, сохраняющих
рёбра, нет: g должно переводить набор ходов {l, r, x} в себя; сопряжение
сохраняет порядок элемента, так что x переходит в x, а l — в l или r;
такие g — это только id_n и sigma_n (перебором подтверждено при 4 <= n <= 7).

Оба преобразования годятся для группировки данных по расстоянию. Обращение
нельзя использовать как автоморфизм: ни в рассуждениях о рёбрах, ни для
факторизации BFS, ни для автоматической симметризации потенциала.
Инвариантность потенциала к этим преобразованиям не обязательна.

---

## 5. Известные оценки

Статусы всех оценок — в CLAIMS.md. Формула в этом разделе сама по себе
не означает завершённого локального аудита.

### 5.1. Прежние оценки

Базовая конструкция из входных файлов (C1), p = floor(n^2/4):

```text
A_n  = 2p + n - (2p + 11n - 11)/(3n)
D_n <= floor(A_n) при n >= 4;   следствие: D_n <= B_n + floor(4n/3) - 3

n            4    5    6    7    8
floor(A_n)   8   13   19   26   35
D_n          6   10   15   21   28
запас        2    3    4    5    7
```

Запас растёт линейно, примерно как 4n/3. Gap-файл заявляет floor(A_n) - 1
всегда, - 2 при n >= 138, - 3 при n >= 852 (C4, C10, C11).

S2: `d(sigma_n) <= B_n` через явное разложение (C9); `D_n <= B_n + 3n`;
алгоритм с эмпирически ожидаемой длиной B_n + n/2.

Прежняя гипотеза H4 связывала слагаемое 2p с числом обменов. Само по себе
слагаемое итоговой оценки не определяет N_X: обмены и вращения нужно считать
по построенному слову с учётом сокращений.

### 5.2. Новая верхняя оценка N1

Источник N1 — `docs/incoming/LRX_STRICT_PROOF_RU-1.md`. Самодостаточный текст
доказательства прошёл локальный аудит 12.09.2026 (`docs/notes/strict_proof_audit.md`,
чекер `checks/check_C16.py`): пробелов не найдено. Теорема — C16, упрощённое
следствие — C17, статус обоих PROVED (локальный аудит одной сессии).

При n >= 4 положим:

```text
P       = floor(n^2/4)                         (то же число, что p в §5.1)
r_n     = (sqrt(3n^2 - 4n + 1 - 4P) - (n-1))/2
R_n     = 2P + n - 2 - 2P/(3n) + 2/n - 7r_n/9
U_n     = floor(R_n)
kappa   = (31 - 7sqrt(2))/18 ≈ 1.1722502813

(Т)     D_n <= U_n
(Т′)    D_n <= B_n + floor(kappa*n) - 2       при нечётном n
        D_n <= B_n + floor(kappa*n) - 1       при чётном n
```

Линейный коэффициент остатка уменьшается с 4/3 до kappa; остаток остаётся
линейным. Ни `D_n <= B_n + n`, ни `D_n = B_n` из N1 не следуют.
Новая формула не доминирует старую при каждом n:

| n | B_n | floor(A_n) | U_n |
|---|---|---|---|
| 4 | 6 | 8 | 9 |
| 7 | 21 | 26 | 27 |
| 20 | 190 | 213 | 211 |
| 100 | 4950 | 5079 | 5065 |

Это точное сравнение формул, а не вычисление диаметров. Прежние оценки
C1/C4/C10/C11 сохраняются. Удостоверенная граница — минимум применимых
PROVED-оценок и VERIFIED-данных в пределах их конечного диапазона.
Минимум заявленных формул
показывается отдельно с их зависимостями; CLAIMED не превращается в PROVED
из-за взятия минимума.

Обозначения N1 для n >= 4:

- `f_c(i) = pi(i) + c mod n`; черта — среднее по всем c из Z_n.
- Для нетривиального цикла C: подписанные кратчайшие шаги удовлетворяют
  `-n/2 < d_j <= n/2`; антиподальный шаг всегда положителен. F(C) — сумма
  длин дуг, M(C) — максимальная нагрузка неориентированного ребра,
  t(C) — число переходов отрицательного шага в положительный,
  `E(C) = |C| - 2t(C)`, `S(C) = 2M(C) + E(C) - 1`.
- F_c, E_c, S_c — суммы по нетривиальным циклам f_c; `s = overline(S_c)`,
  `mu = overline(sum_C M(C))`, theta_c — число антиподальных транспозиций.
- `H(0) = n`, `H(c) = n + delta_n(0,c) - 2` при c != 0 — достаточная
  стоимость внешнего позиционирования; фактический маршрут может быть короче.
- `tau(i) = -pi(i) mod n`, `I_i = {i+1,...,i+floor(n/2)} mod n`,
  `e_i = |tau(I_i) triangle I_tau(i)|`, где triangle — симметрическая разность;
  `V = sum_i e_i`, `x = V/n`, `T_h = sum_i delta_n(pi(i)+i,h)`,
  `T_min = min_h T_h`, `mathcal E = sum_c E_c`.

Цепочка N1: локальные слова и сшивка (C18) дают

```text
d(pi) <= min_c [2F_c - S_c + H(c)]
      <= 2P + n - 2 + (P+2)/n - s.                     (N1, 3.4)
```

Лемма опорной точки (C19), два разреза (C20) и цикловая оценка (C21)
используются для нижней оценки s; исключение параметров и округление дают C16.
Гипотеза `2mu + overline(E_c) >= n-1` (C22) в этот вывод не входит.

### 5.3. Ограничение усреднённого выражения на отражениях

Утверждение C24 относится к выражению (N1, 3.4), а не к минимально возможной
длине слова. Пусть `pi(i) = h-i mod n`, в частности pi = sigma_n при h = 1.
Тогда каждый f_c — инволюция, состоящая из неподвижных точек и транспозиций.

У неантиподальной транспозиции выбранные дуги проходят один и тот же путь
в противоположных направлениях: M = 2, E = 0, S = 3. У антиподальной обе
дуги положительны: M = 1, E = 2, S = 3. Для каждого i ровно один c делает
i неподвижной точкой f_c. Среднее число неподвижных точек равно 1, среднее
число транспозиций — (n-1)/2. Следовательно, `s = 3(n-1)/2` точно.

При этом само усреднённое выражение равно:

```text
2P + n - 2 + (P+2)/n - s
  = B_n + P/n - 1/2 + 2/n          при чётном n
  = B_n + P/n - 1   + 2/n          при нечётном n
  = B_n + n/4 + O(1).
```

Вывод: даже точное s не позволяет получить B_n из этого выражения для всех n.
Чтобы достичь C15, недостаточно усиливать только оценку s в N1 §§5–8.
Нужно также улучшать минимум по c вместо среднего, позиционирование,
сокращения или саму конструкцию. Это не нижняя оценка длины алгоритма
и не препятствие точной сортировке той же конструктивной идеей.

### 5.4. Отражения: `d(pi_h) <= B_n` (C28, сессия 4)

Для отражений `pi_h(i) = h − i` вариант конструкции N1 с маршрутом только по
переносчикам (правило 16; локальные слова и сшивка — N1 §§1–3) даёт при
явно указанном сдвиге слово длины `<= B_n + 1`, а после свободного
сокращения `<= B_n`; доказательство — `docs/proofs/C28_reflections_pr5.md`
(F_c, S_c отражений явно по чётности; маршрут в длинную сторону
`R_c <= n − delta_n(0, c)`). Следствие: `d(sigma_n) <= B_n` при всех n >= 4
(C9 — теперь PROVED локально), `d(rev_n) <= B_n`. Равенство `d(sigma_n) = B_n`
по-прежнему требует нижней оценки C8. Тот же вариант на других входах
`B_n + O(1)` не даёт: аффинные входы `pi(i) = a·i + b` (n >= 17) — CLAIMS C27.

### 5.5. Отражения: `d(pi_h) <= B_n − delta_n(h, 1)` (C32, сессия 7)

Отражение позиций `i -> h' − i` (h' = h + c) переводит в себя каждую из двух
дуг Z_n с центрами в точках оси h'/2 и h'/2 + n/2, обращая порядок; поэтому
сортировка pi_h — две независимые линейные инверсии дуг длин a + b = n,
`N_X = a(a−1)/2 + b(b−1)/2`, минимум `floor((n−1)^2/4)` при сбалансированных
дугах (чётность дуги задаётся типом точки оси: вершина — нечётная, середина
ребра — чётная). Линейная инверсия делается зигзагом с обменами только на
соседних рёбрах (одно вращение между обменами); первая дуга — из центра,
вторая — с края к центру, стык 2 шага, финальная прогулка к c. Итог:
`N_rot = floor(n^2/4) − delta_n(h, 1)`, `len = B_n − delta_n(h, 1)` при всех
n >= 4 и всех h (`docs/proofs/C32_two_arc_reversal.md`,
`constructions/reflection_word.py`, `checks/check_C32.py`). Следствия:
C9 элементарно, `d(rev_n) <= B_n − 2`; C31 (`d(pi_h) = B_n − delta_n(h, 1)`)
равносильна C8. Два граничных ребра между дугами не используются: слово живёт
в линейной модели с одним разрезом (гипотеза H13, PLAN §0).


### 5.6. Линейная модель и профиль по инверсиям (сессия 8, конечные данные)

Двойной разрез (q, c): линия `w_j = pi(q+1+j) − (q+1−c) mod n`; `I(pi) =
min_{q,c} inv(w)`. Данные (C33, C35, C36; `docs/notes/h13_line_model.md`):
`I(pi) <= floor((n−1)^2/4)` и `d(pi) <= I(pi) + floor(n^2/4)` при 4 <= n <= 10
(VERIFIED; равенство на sigma_n и его соседях; вместе по C12 это `D_n <= B_n`);
при 4 <= n <= 9 нужный `N_rot` при `N_X = I(pi)` не превосходит `floor(n^2/4)`
и достигает его не только на отражениях. Доказано (C34): любая линия
сортируется маршрутом «коктейль» с `N_X = inv`, `N_rot = B_n − 1` (точно);
антиподальная транспозиция имеет `I = 1`, `d = n + 1` (чётные n), поэтому
оценка вида `d <= 2I + g(n)` требует `g(n) >= n − 1`.

Сессия 9: `I(pi) <= floor((n−1)^2/4)` (H13-I) не доказана, но диапазон
исчерпывающей проверки расширен до 4 <= n <= 13 (`experiments/line_profile_fast.c`,
`data/runs/line_profile_fast/`); найдены точное тождество для суммы по
всем n^2 разрезам и симметрия `I(pi) = I(pi^{-1})`; два семейства с одной
степенью свободы (поворот только позиций, только значений) опровергнуты
как замена H13-I. Вероятный источник доказательства — S3 (не аудирован).
Подробности — `docs/notes/h13_line_model.md` §7.

---

## 6. Внешние источники и входные файлы

### 6.1. Источники

| ID | Источник и версия | Значение для проекта |
|---|---|---|
| S1 | G. V. Antiufeev, A Lower Bound for the Diameter of Cayley Graph of the Symmetric Group S_n Generated by (12), (12…n), (1n…2). arXiv:2601.08715v2, 24 апреля 2026. https://arxiv.org/html/2601.08715v2 | Заявлено доказательство D_n >= B_n для тех же генераторов. Сначала соответствие постановки (перевод конвенций), затем аудит доказательства |
| S2 | Chervov et al., CayleyPy RL: Pathfinding and reinforcement learning on Cayley graphs. arXiv:2502.18663v3, 15 мая 2026. https://arxiv.org/html/2502.18663v3 | Более масштабные вычисления; явное разложение sigma_n; верхняя оценка B_n + 3n; алгоритм с эмпирически ожидаемой длиной B_n + n/2 |
| S3 | R. M. Adin, N. Alon, Y. Roichman, «Circular sorting», arXiv:2502.14398v1 (2025); связанные: arXiv:2608.06338 «Circular sorting in the alternating group»; Develin, Macauley, Reiner, «Toric partial orders», Trans. AMS 368 (2016), 2263–2287 | Найден в сессии 9 (`docs/notes/h13_line_model.md` §7.5) как вероятный источник H13-I: по выдержкам поисковика, «worst-case distance to sort n labeled points on a circle by adjacent swaps is exactly floor((n−1)^2/4)», доказательство — divide-and-conquer по дугам с дискретной теоремой о промежуточном значении. Полный текст не получен (сетевые ограничения среды сессии 9: arxiv.org и смежные домены не проходят через прокси); статус CLAIMED, аудит не начат, точное соответствие конвенций (их «distance» — I(pi) или другая величина) не подтверждено |

Для каждого используемого результата в `docs/notes/literature_audit.md`
фиксируются: версия источника, номер утверждения, точная формулировка,
перевод нотации в проектную, локальный статус аудита.

Результат S1 имеет статус CLAIMED до завершения аудита. Нельзя ни объявлять
его доказанным автоматически, ни писать, что нижняя оценка B_n никем не
заявлена. Найденный пробел указывается как точный переход; непонятное
доказательство не равно опровержению.

Повторный BFS полезен для проверки своей реализации и рабочих таблиц,
даже когда диаметр уже известен из источника.

### 6.2. Входные файлы (`docs/incoming/`, только чтение)

| Файл | Содержание по исходному описанию | Что проверить |
|---|---|---|
| `lrx_2n3_proof_checked.md` | Верхняя оценка C1: циклы, кратчайшие дуги, «карман» (carrier), макросы XL/RX, сокращения XX, усреднение по сдвигам c | Конвенции, логические переходы, область n >= 4 |
| `lrx_2n3_check.py` | Реализация конструкции с проверками лемм; сообщён исчерпывающий прогон n = 4..7 | Воспроизвести прогон, сопоставить код с текстом |
| `lrx_gap_improvement.md` | Улучшения: на 1 ход всегда, на 2 при n >= 138, на 3 при n >= 852; запас на коротких циклах, пропуск большого промежутка | Доказательство, округления, пороги, недостающие машинные проверки |
| `LRX_STRICT_PROOF_RU-1.md` (N1) | Самодостаточное доказательство C16/C17; конструкция, леммы и отдельные гипотезы | Аудит §§1–9, карта зависимостей C18–C21, пределы §10, происхождение прогонов §12 |
| `verify_lrx.py` | В N1 §12 сообщена независимая проверка конструкции и лемм | Файл не приложен к текущему комплекту; найти в проекте, затем проверить версию и воспроизвести |
| `verification.json` | В N1 §12 сообщён результат PASS | Файл не приложен к текущему комплекту; сверить с кодом, командой и покрытием, не считать локальным прогоном заранее |

Во входном описании базовое доказательство отмечено как проверенное —
это происхождение результата, а не локальный аудит. Отсутствие файла
блокирует аудит соответствующей конструкции, но не ядро, малые n и чтение
внешних источников. Содержимое отсутствующих файлов по описанию не
восстанавливается.

С 12.09.2026 (коммит 972890f) в `docs/incoming/` лежат N1 и прежний комплект
(`lrx_2n3_proof_checked.md`, `lrx_2n3_check.py`, `lrx_gap_improvement.md`);
`verify_lrx.py` и `verification.json` не переданы. Состояние аудита каждого
файла — `docs/incoming/README.md`.

N1 называет `LRX_60_MINUTE_REPORT.md` своим исходным материалом, но не использует
его или прежний комплект как предпосылки доказательства §§1–9. Отсутствие
старых файлов не блокирует аудит N1. Формулировки (16?) и (17?) исходного отчёта
не восстанавливаются по названиям: в N1 полностью записана гипотеза C22;
для точной регистрации (17?) и порогового варианта нужен исходный текст.

В N1 §12 сообщены: все 144 606 нетривиальных ориентированных циклов
при 4 <= n <= 9; все 5 904 перестановки при 4 <= n <= 7; 160 дополнительных
перестановок восьми порядков от 8 до 100; скалярные проверки округлений
при 4 <= n <= 100000. Для порядков 50 и 100 полные слова не строились.
Исчерпывающая, выборочная и скалярная части имеют разное покрытие и должны
учитываться отдельно. До получения артефактов и воспроизведения это сообщение
источника, а не локальные VERIFIED-результаты.

---

## 7. Потенциалы: установленные факты

Определение. Потенциал — функция Phi на S_n. Допустимый потенциал:
`Phi(id_n) = 0` и `|Phi(p) - Phi(q)| <= 1` на каждом ребре.
Тогда `Phi(p) <= d(p)` — индукция вдоль кратчайшего пути.

- Точная d — наибольший допустимый потенциал. Существование какого-то
  потенциала не заменяет явной полезной формулы.
- min, max и любое выпуклое среднее допустимых потенциалов допустимы.
  `max(Phi_1, Phi_2)` не слабее каждого из них — это инструмент сборки оценки
  из слабых частей. Обратное не работает: комбинация недопустимых частей
  допустимой не становится, нужен общий доказанный контроль изменений.
- Усреднение допустимого потенциала по сопряжению sigma_n сохраняет
  допустимость (автоморфизм). Для обращения такой гарантии нет.
- Число инверсий недопустимо: при L оно может измениться на n - 1.
- Потенциал, не зависящий от положения головы (`Phi(pL) = Phi(p)`), не
  достигает B_n уже при n = 4: `apply_word(sigma_4, "LL") = (3,2,1,0)`, а d этой
  вершины равно 4. Нельзя стирать всю информацию об ориентации относительно головы.
- `F_c(p) = sum_i delta_n(i, (p[i] + c) mod n)`; `F_c(pL) = F_{c+1}(p)`, поэтому
  `min_c F_c` не зависит от положения головы. Отдельный F_c при X меняется не
  больше чем на 2, при L — до n. Это возможная часть оценки, а не решение.
