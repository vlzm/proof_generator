# H13 — линейная модель: профиль, бюджет вращений, вердикт

Дата: 13.09.2026 (сессия 8). Ядро oracle-1.0, таблицы C2v (4 <= n <= 10).
Код: `experiments/line_profile.c`, `experiments/line_rotation_budget.py`,
`experiments/line_model.py` (1.1), `constructions/line_cocktail.py`,
`checks/check_C34.py`. Отчёты: `data/runs/line_profile/`,
`data/runs/line_rotation_budget/`, `data/runs/line_model/`,
`data/runs/check_C34/`.

## 0. Определения

Двойной разрез `(q, c)`: ребро позиций {q, q+1} и сдвиг c. Линия
`w_j = pi(q+1+j) − (q+1−c) mod n`, j = 0..n−1; цель — тождественная линия;
голова стартует в индексе `j0 = (−q−1) mod n`, заканчивает в `j1 = (c−q−1)
mod n`. Слово *линейное*, если голова не переходит и не обменивает через
разрез; *сокращённое*, если каждый обмен снимает инверсию (`N_X = inv(w)`).

- `I(pi) = min_{q,c} inv(w)` — по всем n^2 разрезам (эквивалентно: минимум
  инверсий по торическому классу pi — повороты позиций и значений).
- `d_line(pi)`, `d_walk(pi)`, `d_red(pi)`, `Rbest(pi)`, `Rfree(w)` — см.
  отчёты `line_model`, `line_rotation_budget`. Всегда
  `d <= d_walk <= d_line <= d_red`.

## 1. Что показали данные (исчерпывающе)

1. `I(pi) <= floor((n−1)^2/4)` при 4 <= n <= 10 (равенство — ровно n отражений).
2. `d(pi) <= I(pi) + floor(n^2/4)` при 4 <= n <= 10; более того,
   `d_red(pi) <= I(pi) + floor(n^2/4)` при 4 <= n <= 9: для каждой pi есть
   разрез с `inv = I(pi)` и сокращённое линейное слово с
   `N_rot <= floor(n^2/4)`. Максимум `Rbest = floor(n^2/4)` достигается на
   2, 4, 11, 11, 50, 111 входах (n = 4..9) — не только на отражениях.
3. Профиль `max_{I(pi) = I} d(pi)` (профиль `line_profile`): при I >= max − 1
   равен `I + floor(n^2/4)`, при n <~ I <= max − 2 — `I + floor(n^2/4) − 1`,
   при малых I — около `2I + n`. Бюджет вращений `floor(n^2/4)` требуется
   уже при I ≈ n.
4. `max_pi (d − 2I)` = 3, 3, 5, 6, 8, 10, 13 при n = 4..10: растёт не медленнее
   n − 1 (лемма B) и быстрее n (обмен блоков у антипода).
5. `max_w Rfree(w) = B_n − 1` при 4 <= n <= 9 — без выбора разреза бюджет
   вращений равен `B_n − 1` (лемма A, точно).
6. `d_walk = d` при n <= 7; при n = 8, 9 есть входы с `d_walk − d` до 4, 5
   (аффинные), но при `I >= max − 1` всегда `d_line = d`.
7. Индукция по одному элементу для п. 1 не проходит: при n = 8 аффинный вход
   `(0,5,2,7,4,1,6,3)` имеет I = 11, а все восемь его редукций (удаление
   элемента с контракцией позиций и значений) — I = 6, тогда как
   `floor((n−1)^2/4) − floor((n−2)^2/4) = floor((n−1)/2) = 3`. Семейство
   разрезов «первый элемент линии имеет значение t» (n разрезов вместо n^2)
   тоже недостаточно ни при каком t (n = 5: `(0,2,4,1,3)`; n = 7:
   `(0,2,4,6,1,3,5)`).

## 2. Лемма A (универсальный маршрут линии; PROVED)

Для любой линии w из n позиций существует сокращённое линейное слово с
`N_X = inv(w)` и `N_rot = B_n − 1` (конец головы свободен; старт — ребро 0).
Оценка точна: полностью обращённая линия имеет `inv = B_n`, а в кратчайшем
слове `N_rot >= N_X − 1`.

Маршрут (`constructions/line_cocktail.py`): проход 1 по рёбрам 0, 1, ..., n−2;
проход 2 по n−3, ..., 0; проход 3 по 1, ..., n−4; проход 4 по n−5, ..., 1; …
(k-й проход содержит n − k рёбер, направления чередуются, каждый проход
начинается рядом с концом предыдущего). На каждом ребре обмен делается
тогда и только тогда, когда пара инвертирована. Всего `1 + 2 + … + (n−1) =
B_n` сравнений, длина пути `B_n − 1`.

Корректность: проход 1 переносит максимум на позицию n−1; проход 2 —
минимум позиций 0..n−2 на позицию 0; проход 3 — максимум позиций 1..n−2 на
n−2; и т. д. (шейкер-сортировка с сужающимися границами). После k проходов
`ceil(k/2)` наибольших и `floor(k/2)` наименьших элементов стоят на своих
местах, после n − 1 проходов — все. Каждый обмен снимает ровно одну инверсию,
поэтому обменов ровно `inv(w)`. ∎ Проверено эталонными ходами на всех линиях
при 4 <= n <= 8 (`checks/check_C34.py`, часть A).

Следствие. Для любой pi и любого разреза `d(pi) <= inv(w) + B_n − 1 + подход +
финал`. Это бесполезно для C15: вся экономия до `floor(n^2/4)` — в выборе
разреза (п. 1.5), а не в маршруте по данной линии.

## 3. Лемма B (антиподальная транспозиция; PROVED при чётных n >= 6, таблицы 4..10)

Пусть n = 2m чётно и `tau_n` — тождественная перестановка с обменянными
позициями m, m+1. Тогда `I(tau_n) = 1` и `d(tau_n) = n + 1`.

`I = 1`: разрез q = n−1, c = 0 даёт линию с одной инверсией; `I = 0` только у
поворотов тождественной. Верхняя оценка — слово `L^m X R^m` длины n + 1.

Нижняя оценка. Пусть слово сортирует tau_n и голова заканчивает в c; тогда
элемент e должен оказаться на позиции e + c. Для элемента e обозначим `t_e`
его суммарное знаковое смещение по обменам (каждый X даёт +1 одному и −1
другому участнику); `sum_e t_e = 0`, `N_X >= (1/2) sum_e |t_e|`, и
`t_e ≡ c (mod n)` при e ∉ {m, m+1}, `t_m ≡ c − 1`, `t_{m+1} ≡ c + 1`.

Случай c = 0. Если есть обмен на ребре m (антипод старта), голова проходит
0 → m → 0: `N_rot >= 2m = n`, `N_X >= 1`, итого >= n + 1. Если обменов на ребре
m нет, все обмены — соседние транспозиции пути `m+1, m+2, …, n−1, 0, …, m`
(окружность без ребра m), и их произведение как перестановка позиций равно
транспозиции концов этого пути, длина которой в порождающих пути равна
`2n − 3 >= n + 1`.

Случай c ≠ 0. Автоморфизм `p -> sigma_n o p o sigma_n` (PROBLEM §4.3) меняет
L и R местами, сохраняет tau_n (проверка: 1 − tau(1 − i) = i при i ∉ {m, m+1}
и меняет m, m+1 местами) и переводит c в −c; поэтому достаточно 1 <= c <= m.
Запишем `t_e = r_e + n·k_e`, `r_e` — остаток в [0, n): `sum r_e = (n−2)c +
(c−1) + (c+1) = nc`, значит `sum k_e = −c`. Стоимость `|t_e|` при k = 0 равна
r, при k = −1 — n − r, каждый следующий шаг |k| добавляет n; k > 0 стоит
n + r и требует лишнего уменьшения у другого элемента. Минимум `sum |t_e|`
достигается, когда ровно c элементов имеют k = −1 — те, у кого остатки
наибольшие: один с остатком c + 1 и c − 1 с остатком c:
`sum |t_e| >= nc + (n − 2c − 2) + (c − 1)(n − 2c) = 2cn − 2c^2 − 2`,
`N_X >= cn − c^2 − 1`. При 1 <= c <= m правая часть вогнута по c, на концах
`n − 2` (c = 1) и `m^2 − 1 >= n − 2` (c = m), так что `N_X >= n − 2`. В
кратчайшем слове нет XX, поэтому `N_rot >= N_X − 1` и длина `>= 2n − 5 >= n + 1`
при n >= 6. ∎ При n = 4 равенство `d = 5` — по таблице; `d = n + 1` при
n = 4, 6, 8, 10 сверено с таблицами (`check_C34.py`, часть B).

Следствие. Всякая оценка вида `d(pi) <= 2·I(pi) + g(n)`, верная при всех pi,
требует `g(n) >= n − 1` (чётные n). Критерий PLAN §8 (`g(n) <= floor(n/2) +
O(1)`) недостижим. Обмен блоков длины k у антипода даёт ещё больше:
`d − 2I` = 8 (n = 8, k = 2), 13 (n = 10, k = 3).

## 4. Лемма C (локальная оптимальность разреза)

Если (q, c) минимизирует inv, то линия w удовлетворяет
`sum_{j<k} w_j <= k(n−1)/2` для всех k (и симметрично для суффиксов), и то же
для обратной перестановки (`sum_{x<k} pos(x) <= k(n−1)/2`): сдвиг разреза
позиций на k переносит первые k элементов в конец, меняя inv на
`sum_{j<k} (n − 1 − 2 w_j)`; сдвиг разреза значений на k делает элементы
0..k−1 наибольшими, меняя inv на `sum_{x<k} (n − 1 − 2 pos(x))`. ∎

Этих условий недостаточно для `inv <= floor((n−1)^2/4)`: линия
`(0,5,4,3,2,1,6)` при n = 7 удовлетворяет всем им (суммы 0, 5, 9, 12, 14, 15
при порогах 0, 6, 9, 12, 15, 18; обратная — те же), но `inv = 10 > 9`; её
торический класс имеет I = 8 при несоседнем разрезе. Доказательство п. 1.1
должно использовать совместные сдвиги обоих разрезов.

## 5. Вердикт по H13

1. Исходная форма H13 («один неиспользуемый разрез достаточен для диаметра»)
   подтверждена конечно и уточнена: на слоях `B_n`, `B_n − 1` `d_line = d`
   при 4 <= n <= 9, но в общем случае обмены через все рёбра бывают нужны
   (n = 8, 9).
2. Критерий PLAN §8 — оценка `d <= 2I + g(n)` с `g(n) <= floor(n/2) + O(1)` —
   ложен (лемма B). Правильная форма — с наклоном 1:
   **H13′: `d(pi) <= I(pi) + floor(n^2/4)`**, распадающаяся на
   **H13-I: `I(pi) <= floor((n−1)^2/4)`** (чистая комбинаторика торических
   классов) и **H13-R: существует разрез с `inv = I(pi)` и сокращённое линейное
   слово с `N_rot <= floor(n^2/4)`** (бюджет вращений, не зависящий от I).
   `H13-I + H13-R => C15` по C12. Обе части VERIFIED (4 <= n <= 10 и 4 <= n <= 9),
   обе тесны.
3. «Доказуемая схема слова для линии с inv инверсиями и `N_rot <= inv + f`»
   (PLAN §8 п. (2)) не существует в нужной силе: бюджет вращений не убывает с
   inv (Rbest = floor(n^2/4) при I ≈ n), а без выбора разреза он равен
   `B_n − 1` (лемма A). Схема должна выбирать разрез; экстремальные маршруты
   разного вида (зигзаги на отражениях, холостые переходы на блочных входах).
4. Пункт (3) PLAN §8 (входы с `I > max − n/4`) не сводит задачу к конечному
   числу семейств: их 660 при n = 10 и они растут с n.

## 6. Следующий шаг

Доказать H13-I отдельно (бюджет 1 сессия): утверждение о торических классах —
у каждой pi есть поворот позиций и значений с не более `floor((n−1)^2/4)`
инверсиями. Эквивалентная форма: у каждой pi есть двойной разрез с не менее
`floor(n^2/4)` неинверсий; в терминах точек `(i, pi(i))` на торе — точка
`(q, s)` тора, лежащая в «одноимённых» квадрантах не менее `floor(n^2/4)` пар.
Известно: усреднение по всем n^2 разрезам не работает (среднее для sigma_n
равно `(n^2 − 1)/6 < n^2/4` в форме максимума и `~n^2/3` для id в форме
минимума), индукция по одному элементу не работает (п. 1.7), локальные
условия недостаточны (лемма C). Кандидаты: сдвиги обоих разрезов вместе;
экстремальность отражений через леммы C32 (баланс дуг); литература о
торических классах перестановок. Если H13-I доказана — H13-R остаётся
эквивалентом C15 при известном N_X и переходит в задачу о маршрутах; если
нет за сессию — переход к индукции H3 (PLAN §4.3).

## 7. H13-I: попытка доказательства, сессия 9 (14.09.2026)

Бюджет: 1 сессия, как и предписано в §6. Код:
`experiments/h13i_row_average.c` (h13i_row_average-1.0), `experiments/h13i_probe.py`.
Отчёт: `data/runs/h13i_row_average/report.md`. Рассмотрены (по памяти, без
веб-доступа — запрещён условиями задачи) термины «минимум инверсий
перестановки на торе», «circular/cyclic permutation inversion number»,
«necklace splitting inversions», «bandwidth перестановочной матрицы на
цилиндре»; уверенно опознаваемой точной формулировки и готового доказательства
этой конкретной задачи (двойной разрез: независимый поворот и позиций, и
значений, минимум инверсий) не найдено — считать это отсутствием результата
поиска по памяти, а не строгим отрицательным результатом (полноценный поиск
по литературе не проводился).

### 7.1. Метод: явные градиенты `inv(q,s)` по каждой координате отдельно

Основной кандидат сессии — «совместные сдвиги обоих разрезов» (§6). Чтобы
работать с ними явно, выведены (и проверены) две рекуррентные формулы —
точные конечные разности `inv(q,s)` по каждой координате:

- **По q**: `inv(q+1,s) − inv(q,s) = (n−1) − 2·((pi(q+1) − s) mod n)`.
- **По s**: `inv(q,s+1) − inv(q,s) = (n−1) − 2·((pi^{-1}(s) − q − 1) mod n)`.

Обе получены прямым комбинаторным подсчётом: сдвиг q на 1 циклически
переносит первый элемент строки `w_0` в конец (остальные не меняют
взаимного порядка), и элемент, ранее дававший `j < k` инверсию с каждым
`k`, где `w_k < w_0`, начинает давать инверсию с каждым `k`, где было
`w_k > w_0` — разность обменов равна `(n−1−w_0) − w_0`; сдвиг s на 1
циклически переносит элемент со значением 0 в максимум (аналогично).
Вторая формула — частный случай первой для `pi^{-1}` (симметрия
`I(pi) = I(pi^{-1})`, см. п. 7.1a). Проверены исчерпывающе (все pi, все
`(q,s)`) при 4 <= n <= 7 и на 300 случайных перестановках при n = 8, 9, 12
(`h13i_probe.py`, часть 1) — расхождений нет.

**7.1a. Симметрия `I(pi) = I(pi^{-1})`.** Точки `(i, pi(i))` при транспонировании
координат переходят в точки `(pi(i), i)` — граф `pi^{-1}`; условие
«j < k (по циклическому разрезу q) и w_j > w_k (по разрезу s)» симметрично
относительно обмена ролей «позиция» и «значение» вместе с обменом разрезов
`(q,s) -> (s,q)`. Отсюда `inv_pi(q,s) = inv_{pi^{-1}}(s,q)` и `I(pi) = I(pi^{-1})`
(использовано без отдельной численной проверки — прямое следствие
определения, не новое утверждение реестра).

Эти градиенты дают явную «карту наклонов» `inv` на всём торе `Z_n x Z_n`,
но сама по себе не решает задачу: диагональный шаг `(q,s) -> (q+1,s+1)`
(предложенный в §6 как совместный сдвиг) не имеет такой же простой формулы
разности (она распадается на вклад от переноса `w_0` в конец и вклад от
переноса значения 0 в максимум РАЗНЫХ элементов строки — выражение с двумя
слагаемыми без дальнейшего упрощения). Явного «телескопирующего» аргумента
по диагональным шагам найти не удалось.

### 7.2. Замкнутая форма для `I(q) = min_s inv(q,s)` и новая гипотеза

Зафиксируем q; пусть `a = (a_0,...,a_{n-1})` — соответствующая строка,
`rho = a^{-1}` (позиция каждого значения в строке). Из формулы по s
(суммированием по s от 0) получается:

```
inv(q,0) = inv(a)               (стандартное число инверсий строки a; inv(a) = inv(rho))
R(s) = rho(0) + rho(1) + ... + rho(s-1)          (R(0) = 0)
M(s) = 2*R(s) - s*(n-1)
inv(q,s) = inv(a) - M(s)   для всех s = 0..n-1
I(q) = min_s inv(q,s) = inv(a) - max_{0<=s<=n-1} M(s)
```

(`M(s)` — удвоенный избыток суммы позиций s наименьших значений строки над
«сбалансированным» значением `s(n-1)/2`.) Проверено против прямого перебора
по s исчерпывающе при 4 <= n <= 7 и выборочно (200 случайных перестановок)
при n = 8, 9 — расхождений нет (`h13i_probe.py`, часть 2).

Это дало естественный кандидат для совместного аргумента: не усреднять
`inv(q,s)` по s (это уже испробовано и не работает, см. §0 введения задачи
и Введение к заметке), а усреднять по q ИСТИННЫЙ построчный минимум `I(q)`
— величину, для которой уже использован выбор оптимального s. Гипотеза:

> **Гипотеза сессии 9**: `sum_{q=0}^{n-1} I(q) <= n * floor((n-1)^2/4)`.

По принципу Дирихле она СИЛЬНЕЕ H13-I и влечёт её: `min_q I(q) <=
(1/n) sum_q I(q) <= floor((n-1)^2/4)`, а `I(pi) = min_{q,s} inv(q,s) =
min_q I(q)`.

Проверена исчерпывающим перебором всех перестановок (`experiments/h13i_row_average.c`,
таблица в `data/runs/h13i_row_average/report.md`): **0 нарушений при
4 <= n <= 11** (n = 11 — 39 916 800 перестановок за 60 с; заметно шире
диапазона n <= 10, в котором проверена сама H13-I в сессии 8). На каждом
из этих n максимум `sum_q I(q)` достигается РОВНО на `n*floor((n-1)^2/4)`
(не строго меньше) и ровно на входе `pi(i) = -i mod n` (отражение); более
того, на этом входе `I(q) = floor((n-1)^2/4)` для КАЖДОГО отдельного q, не
только в среднем — гипотеза точна в самом сильном смысле на этом семействе.

### 7.3. Три попытки сделать гипотезу конструктивной — все опровергнуты

Если гипотезу §7.2 можно доказать через явное семейство из n (не n^2)
разрезов `{(q, s(q)) : q = 0..n-1}`, для которого `sum_q inv(q,s(q))`
считается замкнуто (как в тождестве 2 из постановки задачи, но по
подмножеству из n пар вместо всех n^2), это дало бы доказательство без
использования истинного минимума. Проверены три естественных кандидата
(`h13i_probe.py`, часть 4; исчерпывающе 4 <= n <= 8):

1. **Диагональ `s = q + t`** для одного общего t (n прямых на торе, по
   одной на каждое t; используется одна и та же прямая для всех pi).
   Опровергнуто: ни при каком t сумма `sum_q inv(q,q+t)` не укладывается
   в `n*bound` для ВСЕХ перестановок сразу — уже при n = 4 у 4 из 24
   перестановок нет ни одного подходящего t среди всех n вариантов.
2. **Семейство «старт = 0»**: `(q,s) = (i-1, pi(i))` для i = 0..n-1 (строка
   всегда начинается со значения 0). Опровергнуто уже на отражениях
   n = 4: сумма равна 12 против границы `n*bound = 8`.
3. **Один фиксированный столбец**: `min_c sum_q inv(q,c)` (минимум по ОДНОМУ
   c, общему для всех q). Опровергнуто почти везде: уже при n = 4 все 24
   перестановки нарушают границу (худшая сумма 14 против 8). Это же
   демонстрирует общее (тривиальное, не численное) неравенство `sum_q
   min_s inv(q,s) <= min_s sum_q inv(q,s)`, которое в данном случае СТРОГОЕ
   и с большим запасом — «выигрыш» гипотезы §7.2 сидит именно в том, что s
   выбирается заново для каждого q, а не общий для всех q.

Все три опровержения — на входах, кратных отражениям (наименьшие
перестановки, на которых нарушение возникает при n = 4, — согласно
правилу 6 AGENTS.md именно эти и есть минимальные контрпримеры в порядке
перебора `itertools.permutations(range(4))`).

### 7.4. Почему доказательство не может быть «по строке»

Лемма C (§4) уже даёт пример: при n = 7 строка, удовлетворяющая условиям
локальной оптимальности разреза s, имеет `inv = 10 > floor(36/4) = 9`
(вход `(0,5,4,3,2,1,6)`, разрез не соседний). Это значит: утверждение
«`I(q) <= floor((n-1)^2/4)` для КАЖДОГО отдельного q» ЛОЖНО — граница
`floor((n-1)^2/4)` может нарушаться на отдельных строках, и выполнение
гипотезы §7.2 в среднем обязано быть эффектом взаимной компенсации между
соседними q (строки с избытком компенсируются строками с запасом), а не
поточечным неравенством. Значит, ни один аргумент вида «докажем `I(q) <=
bound` для произвольного q независимо» не может сработать в принципе —
нужен именно амортизационный (телескопирующий по q, использующий
рекуррентную формулу §7.1) аргумент, а не поточечная оценка. Такой
аргумент не найден за бюджет сессии.

### 7.5. Вердикт

H13-I **не доказана и не опровергнута в этой сессии** (совпадает с
исходом, ожидаемым по условиям задачи). Позитивный результат: найдена и
исчерпывающе проверена при 4 <= n <= 11 новая, более сильная и, по всем
признакам (точность на отражениях, провал трёх естественных попыток
сделать её тривиальной), более «правильная» гипотеза `sum_q I(q) <=
n*floor((n-1)^2/4)` — это и есть первый настоящий результат по направлению
«совместные сдвиги обоих разрезов» (§6), которое было явно указано как
неисследованное. Она сводит H13-I к чисто амортизационному вопросу об
одной переменной q (через явные тождества §7.1), но замкнутого
доказательства для этой более узкой формулировки тоже не найдено.

Не повторять в следующей сессии: усреднение `inv(q,s)` по всем n^2 или n
парам любого фиксированного (не зависящего от pi построчно) вида —
диагонали, столбцы, «старт = 0» — все три опровергнуты явно (§7.3);
поточечную оценку `I(q) <= bound` для отдельного q — ложна (§7.4, Лемма C).
Направление, которое стоит попробовать в первую очередь, если H13-I
возвращается в работу: телескопирование гипотезы §7.2 через рекуррентные
формулы §7.1 (амортизационный потенциал по q) и/или явные градиенты вдоль
диагоналей `(q,q+t)` (посчитаны только суммой, не как потенциал).

Согласно PLAN §8 и указанию задачи этой сессии, следующая задача репозитория
после этого вердикта — индукция H3 (PLAN §4.3, п. 3), а не дальнейшие
варианты усреднения.
