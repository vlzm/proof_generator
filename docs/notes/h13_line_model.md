# H13 — линейная модель: профиль, бюджет вращений, вердикт

Дата: 13.09.2026 (сессия 8). Ядро oracle-1.0, таблицы C2v (4 <= n <= 10).
Код: `experiments/line_profile.c`, `experiments/line_rotation_budget.py`,
`experiments/line_model.py` (1.1), `constructions/line_cocktail.py`,
`checks/check_C34.py`. Отчёты: `data/runs/line_profile/`,
`data/runs/line_rotation_budget/`, `data/runs/line_model/`,
`data/runs/check_C34/`.

## 0. Определения

Двойной разрез `(q, c)`: ребро позиций {q, q+1} и сдвиг c. Линия
`w_j = pi(q+1+j) − (q+1−c) mod n`, j = 0..n−1; цель — тождественная линия;
голова стартует в индексе `j0 = (−q−1) mod n`, заканчивает в `j1 = (c−q−1)
mod n`. Слово *линейное*, если голова не переходит и не обменивает через
разрез; *сокращённое*, если каждый обмен снимает инверсию (`N_X = inv(w)`).

- `I(pi) = min_{q,c} inv(w)` — по всем n^2 разрезам (эквивалентно: минимум
  инверсий по торическому классу pi — повороты позиций и значений).
- `d_line(pi)`, `d_walk(pi)`, `d_red(pi)`, `Rbest(pi)`, `Rfree(w)` — см.
  отчёты `line_model`, `line_rotation_budget`. Всегда
  `d <= d_walk <= d_line <= d_red`.

## 1. Что показали данные (исчерпывающе)

1. `I(pi) <= floor((n−1)^2/4)` при 4 <= n <= 10 (равенство — ровно n отражений).
2. `d(pi) <= I(pi) + floor(n^2/4)` при 4 <= n <= 10; более того,
   `d_red(pi) <= I(pi) + floor(n^2/4)` при 4 <= n <= 9: для каждой pi есть
   разрез с `inv = I(pi)` и сокращённое линейное слово с
   `N_rot <= floor(n^2/4)`. Максимум `Rbest = floor(n^2/4)` достигается на
   2, 4, 11, 11, 50, 111 входах (n = 4..9) — не только на отражениях.
3. Профиль `max_{I(pi) = I} d(pi)` (профиль `line_profile`): при I >= max − 1
   равен `I + floor(n^2/4)`, при n <~ I <= max − 2 — `I + floor(n^2/4) − 1`,
   при малых I — около `2I + n`. Бюджет вращений `floor(n^2/4)` требуется
   уже при I ≈ n.
4. `max_pi (d − 2I)` = 3, 3, 5, 6, 8, 10, 13 при n = 4..10: растёт не медленнее
   n − 1 (лемма B) и быстрее n (обмен блоков у антипода).
5. `max_w Rfree(w) = B_n − 1` при 4 <= n <= 9 — без выбора разреза бюджет
   вращений равен `B_n − 1` (лемма A, точно).
6. `d_walk = d` при n <= 7; при n = 8, 9 есть входы с `d_walk − d` до 4, 5
   (аффинные), но при `I >= max − 1` всегда `d_line = d`.
7. Индукция по одному элементу для п. 1 не проходит: при n = 8 аффинный вход
   `(0,5,2,7,4,1,6,3)` имеет I = 11, а все восемь его редукций (удаление
   элемента с контракцией позиций и значений) — I = 6, тогда как
   `floor((n−1)^2/4) − floor((n−2)^2/4) = floor((n−1)/2) = 3`. Семейство
   разрезов «первый элемент линии имеет значение t» (n разрезов вместо n^2)
   тоже недостаточно ни при каком t (n = 5: `(0,2,4,1,3)`; n = 7:
   `(0,2,4,6,1,3,5)`).

## 2. Лемма A (универсальный маршрут линии; PROVED)

Для любой линии w из n позиций существует сокращённое линейное слово с
`N_X = inv(w)` и `N_rot = B_n − 1` (конец головы свободен; старт — ребро 0).
Оценка точна: полностью обращённая линия имеет `inv = B_n`, а в кратчайшем
слове `N_rot >= N_X − 1`.

Маршрут (`constructions/line_cocktail.py`): проход 1 по рёбрам 0, 1, ..., n−2;
проход 2 по n−3, ..., 0; проход 3 по 1, ..., n−4; проход 4 по n−5, ..., 1; …
(k-й проход содержит n − k рёбер, направления чередуются, каждый проход
начинается рядом с концом предыдущего). На каждом ребре обмен делается
тогда и только тогда, когда пара инвертирована. Всего `1 + 2 + … + (n−1) =
B_n` сравнений, длина пути `B_n − 1`.

Корректность: проход 1 переносит максимум на позицию n−1; проход 2 —
минимум позиций 0..n−2 на позицию 0; проход 3 — максимум позиций 1..n−2 на
n−2; и т. д. (шейкер-сортировка с сужающимися границами). После k проходов
`ceil(k/2)` наибольших и `floor(k/2)` наименьших элементов стоят на своих
местах, после n − 1 проходов — все. Каждый обмен снимает ровно одну инверсию,
поэтому обменов ровно `inv(w)`. ∎ Проверено эталонными ходами на всех линиях
при 4 <= n <= 8 (`checks/check_C34.py`, часть A).

Следствие. Для любой pi и любого разреза `d(pi) <= inv(w) + B_n − 1 + подход +
финал`. Это бесполезно для C15: вся экономия до `floor(n^2/4)` — в выборе
разреза (п. 1.5), а не в маршруте по данной линии.

## 3. Лемма B (антиподальная транспозиция; PROVED при чётных n >= 6, таблицы 4..10)

Пусть n = 2m чётно и `tau_n` — тождественная перестановка с обменянными
позициями m, m+1. Тогда `I(tau_n) = 1` и `d(tau_n) = n + 1`.

`I = 1`: разрез q = n−1, c = 0 даёт линию с одной инверсией; `I = 0` только у
поворотов тождественной. Верхняя оценка — слово `L^m X R^m` длины n + 1.

Нижняя оценка. Пусть слово сортирует tau_n и голова заканчивает в c; тогда
элемент e должен оказаться на позиции e + c. Для элемента e обозначим `t_e`
его суммарное знаковое смещение по обменам (каждый X даёт +1 одному и −1
другому участнику); `sum_e t_e = 0`, `N_X >= (1/2) sum_e |t_e|`, и
`t_e ≡ c (mod n)` при e ∉ {m, m+1}, `t_m ≡ c − 1`, `t_{m+1} ≡ c + 1`.

Случай c = 0. Если есть обмен на ребре m (антипод старта), голова проходит
0 → m → 0: `N_rot >= 2m = n`, `N_X >= 1`, итого >= n + 1. Если обменов на ребре
m нет, все обмены — соседние транспозиции пути `m+1, m+2, …, n−1, 0, …, m`
(окружность без ребра m), и их произведение как перестановка позиций равно
транспозиции концов этого пути, длина которой в порождающих пути равна
`2n − 3 >= n + 1`.

Случай c ≠ 0. Автоморфизм `p -> sigma_n o p o sigma_n` (PROBLEM §4.3) меняет
L и R местами, сохраняет tau_n (проверка: 1 − tau(1 − i) = i при i ∉ {m, m+1}
и меняет m, m+1 местами) и переводит c в −c; поэтому достаточно 1 <= c <= m.
Запишем `t_e = r_e + n·k_e`, `r_e` — остаток в [0, n): `sum r_e = (n−2)c +
(c−1) + (c+1) = nc`, значит `sum k_e = −c`. Стоимость `|t_e|` при k = 0 равна
r, при k = −1 — n − r, каждый следующий шаг |k| добавляет n; k > 0 стоит
n + r и требует лишнего уменьшения у другого элемента. Минимум `sum |t_e|`
достигается, когда ровно c элементов имеют k = −1 — те, у кого остатки
наибольшие: один с остатком c + 1 и c − 1 с остатком c:
`sum |t_e| >= nc + (n − 2c − 2) + (c − 1)(n − 2c) = 2cn − 2c^2 − 2`,
`N_X >= cn − c^2 − 1`. При 1 <= c <= m правая часть вогнута по c, на концах
`n − 2` (c = 1) и `m^2 − 1 >= n − 2` (c = m), так что `N_X >= n − 2`. В
кратчайшем слове нет XX, поэтому `N_rot >= N_X − 1` и длина `>= 2n − 5 >= n + 1`
при n >= 6. ∎ При n = 4 равенство `d = 5` — по таблице; `d = n + 1` при
n = 4, 6, 8, 10 сверено с таблицами (`check_C34.py`, часть B).

Следствие. Всякая оценка вида `d(pi) <= 2·I(pi) + g(n)`, верная при всех pi,
требует `g(n) >= n − 1` (чётные n). Критерий PLAN §8 (`g(n) <= floor(n/2) +
O(1)`) недостижим. Обмен блоков длины k у антипода даёт ещё больше:
`d − 2I` = 8 (n = 8, k = 2), 13 (n = 10, k = 3).

## 4. Лемма C (локальная оптимальность разреза)

Если (q, c) минимизирует inv, то линия w удовлетворяет
`sum_{j<k} w_j <= k(n−1)/2` для всех k (и симметрично для суффиксов), и то же
для обратной перестановки (`sum_{x<k} pos(x) <= k(n−1)/2`): сдвиг разреза
позиций на k переносит первые k элементов в конец, меняя inv на
`sum_{j<k} (n − 1 − 2 w_j)`; сдвиг разреза значений на k делает элементы
0..k−1 наибольшими, меняя inv на `sum_{x<k} (n − 1 − 2 pos(x))`. ∎

Этих условий недостаточно для `inv <= floor((n−1)^2/4)`: линия
`(0,5,4,3,2,1,6)` при n = 7 удовлетворяет всем им (суммы 0, 5, 9, 12, 14, 15
при порогах 0, 6, 9, 12, 15, 18; обратная — те же), но `inv = 10 > 9`; её
торический класс имеет I = 8 при несоседнем разрезе. Доказательство п. 1.1
должно использовать совместные сдвиги обоих разрезов.

## 5. Вердикт по H13

1. Исходная форма H13 («один неиспользуемый разрез достаточен для диаметра»)
   подтверждена конечно и уточнена: на слоях `B_n`, `B_n − 1` `d_line = d`
   при 4 <= n <= 9, но в общем случае обмены через все рёбра бывают нужны
   (n = 8, 9).
2. Критерий PLAN §8 — оценка `d <= 2I + g(n)` с `g(n) <= floor(n/2) + O(1)` —
   ложен (лемма B). Правильная форма — с наклоном 1:
   **H13′: `d(pi) <= I(pi) + floor(n^2/4)`**, распадающаяся на
   **H13-I: `I(pi) <= floor((n−1)^2/4)`** (чистая комбинаторика торических
   классов) и **H13-R: существует разрез с `inv = I(pi)` и сокращённое линейное
   слово с `N_rot <= floor(n^2/4)`** (бюджет вращений, не зависящий от I).
   `H13-I + H13-R => C15` по C12. Обе части VERIFIED (4 <= n <= 10 и 4 <= n <= 9),
   обе тесны.
3. «Доказуемая схема слова для линии с inv инверсиями и `N_rot <= inv + f`»
   (PLAN §8 п. (2)) не существует в нужной силе: бюджет вращений не убывает с
   inv (Rbest = floor(n^2/4) при I ≈ n), а без выбора разреза он равен
   `B_n − 1` (лемма A). Схема должна выбирать разрез; экстремальные маршруты
   разного вида (зигзаги на отражениях, холостые переходы на блочных входах).
4. Пункт (3) PLAN §8 (входы с `I > max − n/4`) не сводит задачу к конечному
   числу семейств: их 660 при n = 10 и они растут с n.

## 6. Следующий шаг (до сессии 9)

Доказать H13-I отдельно (бюджет 1 сессия): утверждение о торических классах —
у каждой pi есть поворот позиций и значений с не более `floor((n−1)^2/4)`
инверсиями. Эквивалентная форма: у каждой pi есть двойной разрез с не менее
`floor(n^2/4)` неинверсий; в терминах точек `(i, pi(i))` на торе — точка
`(q, s)` тора, лежащая в «одноимённых» квадрантах не менее `floor(n^2/4)` пар.
Известно: усреднение по всем n^2 разрезам не работает (среднее для sigma_n
равно `(n^2 − 1)/6 < n^2/4` в форме максимума и `~n^2/3` для id в форме
минимума), индукция по одному элементу не работает (п. 1.7), локальные
условия недостаточны (лемма C). Кандидаты: сдвиги обоих разрезов вместе;
экстремальность отражений через леммы C32 (баланс дуг); литература о
торических классах перестановок. Если H13-I доказана — H13-R остаётся
эквивалентом C15 при известном N_X и переходит в задачу о маршрутах; если
нет за сессию — переход к индукции H3 (PLAN §4.3).

## 7. Сессия 9 (17.09.2026) — вердикт по H13-I

Бюджет: 1 сессия, как назначено. Итог: **H13-I остаётся CONJECTURED**; ни
доказательство, ни контрпример не найдены. Ниже — новое за эту сессию (не
повторяет п. 1–6): точная аналитическая формула для `inv(q,c)` через XOR трёх
битовых индикаторов (лемма D, PROVED, с независимой численной сверкой),
доказанная негодность двух новых кандидатных стратегий (гибридное усреднение
и «диагональное» ограничение, оба сводятся к уже отвергнутым идеям или к
самому определению I), и расширение исчерпывающей проверки с `n <= 10` до
`n <= 12` новым алгоритмом на порядок быстрее прежнего.

### 7.1. Лемма D (точная формула инверсий разреза; PROVED)

Перепараметризуем разрез: `s = q+1` (точка начала чтения позиций),
`t = q+1−c` (сдвиг значений); `(q,c) <-> (s,t)` — биекция Z_n x Z_n на себя.
Для пары позиций `i < j` определим три бита:

```text
P_{ij}(s) = 1 iff s in (i, j]                                (разрез позиций режет пару)
Q_{ij}(t) = 1 iff t in (min(pi(i),pi(j)), max(pi(i),pi(j))]   (разрез значений режет пару)
V_{ij}    = 1 iff pi(i) > pi(j)                               (пара — инверсия самой pi)
```

**Утверждение.** `inv(w) = sum_{i<j} (P_{ij}(s) XOR Q_{ij}(t) XOR V_{ij})`.

Доказательство. Пусть `j' = (i−s) mod n`, `k' = (j−s) mod n` — индексы чтения
позиций i, j (голова начинает в s); `y_i = (pi(i)−t) mod n` — значение w в
точке i. Инверсия пары — это `(j'−k')` и `(y_i−y_j)` разных знаков. Прямой
разбор (i<j как целые, 0<=i<j<=n−1): `j' < k'` (порядок не переставлен) iff
`s не in (i,j]`, т. е. `P=0`; `y_i > y_j` iff `t in (u,v]`, где `u=min(pi(i),
pi(j))`, если `pi(i)=u` (не переставлено, `V=0`), и iff `t не in (u,v]`, если
`pi(i)=v` (переставлено, `V=1`) — это `Q XOR V`. Собирая: инверсия iff
`(P=0 и Q XOR V=1)` или `(P=1 и Q XOR V=0)`, т. е. `P XOR Q XOR V = 1`. ∎
Численно сверено на 200 случайных (pi, q, c) при 3 <= n <= 9 (все три —
формула, прямое определение и независимая переформулировка — совпали;
скрипт временный, не входит в репозиторий, воспроизводится по тексту выше).

Следствие (объясняет, почему предыдущее «усреднение» и лемма C неполны):
слагаемое `V_{ij}` — это ровно исходные инверсии pi, и оно не является
«разрезом» ни по какому одному порогу (в отличие от `P` и `Q`, каждое из
которых — индикатор биекции {i,j} с порогом одного множества). Пытаться
искать `(s,t)`, устраняющие `V`, значит искать разрез, чей узор `P XOR Q`
приближает узор `V` — а `V` в общем случае произвольная транзитивная
турнирная структура (сам порядок pi), не сводимая к порогу.

### 7.2. Провал двух новых кандидатов

**(a) Гибридное усреднение `min_s (avg_t inv(s,t))`.** Раз `I(pi) = min_s
min_t`, а полное усреднение `avg_s avg_t` не годится (PLAN §6), проверена
промежуточная идея: усреднить только по t (аналитически, через долю `b_{ij}
= |pi(i)−pi(j)|` в лемме D), затем минимизировать по s точно. Это сильнее
двойного усреднения (сохраняет свободу выбора s), но всё ещё слабее полного
`I(pi)`. Численно на отражениях: `min_s avg_t` = 13, 17.5, 22.67, 28.5, 35 при
n = 7..11 против `floor((n−1)^2/4)` = 9, 12, 16, 20, 25 — не годится, причём
хуже общего усреднения (PLAN §6) не становится, но и не приближается к цели.
Причина видна из леммы D: сдвиг s на 1 меняет статус `P_{ij}` сразу для всех
`n−1` пар, содержащих ровно одну фиксированную позицию — это ровно та же
структура «удалить один элемент», что и в отвергнутой индукции (п. 1.7,
`docs/notes/h13_line_model.md` §1), поэтому усреднение по t с точным
минимумом по s страдает тем же дефектом.

**(b) «Диагональное» ограничение `t = ±s + k`.** Идея: для отражений видна
симметрия сдвига обеих координат разреза одновременно (ось `h'=h+c`), значит,
возможно, для произвольной pi достаточно искать по одной «диагонали»
`t ≡ ±s (mod n)` с подходящим сдвигом k, а не по всем n^2 парам. Проверка
показала вырождение: перебор k по всем n значениям при фиксированном знаке
даёт ровно исходный полный перебор n^2 пар (k пробегает все сдвиги), так что
«ограничение» совпадает с определением I(pi) и не даёт независимой оценки.
Настоящее ограничение — с k, вычисляемым из pi *без* полного перебора
(например, простым инвариантом) — эквивалентно уже отвергнутому семейству
«первое значение линии равно фиксированному t» (§1.7 выше) и по тем же
контрпримерам (n = 5, 7) не работает.

### 7.3. Расширение исчерпывающей проверки: n <= 10 -> n <= 12

Новый алгоритм (`experiments/line_toric_max_i.c`, независимая реализация, не
делит код с `line_profile.c`): для фиксированного q инкрементальный пересчёт
`inv` при сдвиге c на 1 за O(1) (лемма: `inv(shift+1) − inv(shift) = n−1−2p`,
`p` — позиция элемента со значением `shift` во вращённой линии; тот же факт,
что и в леммах C28/C32 про подписанные смещения, здесь — для сдвига значений
одной линии). Сложность на одну pi падает с O(n^4) до O(n^3); измерено: n=10
— 5.4 с (было 93 с у `line_profile.c`), n=11 — 73.5 с (новый результат),
n=12 — см. ниже. Корректность алгоритма проверена независимым Python-перебором
O(n^4) при 4 <= n <= 8 и сверкой с прежними `profile_n{9,10}.json` —
`checks/check_H13I.py`, часть A/B, PASS.

**Результат n = 11 (исчерпывающе, все 39 916 800 перестановок):**
`max_pi I(pi) = 25 = floor(10^2/4)`, достигается ровно на 11 входах (все
отражения) — H13-I VERIFIED расширена до `4 <= n <= 11`.

**Результат n = 12** (исчерпывающе, все 479 001 600 перестановок, ≈1055 с
(≈17.6 мин) на одном ядре): `max_pi I(pi) = 30 = floor(11^2/4)`, достигается
ровно на 12 входах (все отражения) — H13-I VERIFIED расширена исчерпывающе
до `4 <= n <= 12`. Команда: `gcc -O2 -o line_toric_max_i
experiments/line_toric_max_i.c && ./line_toric_max_i 12 20000000`; полный лог
и JSON — `data/runs/h13i_verdict/`.

### 7.4. Целевой (не исчерпывающий) поиск контрпримера, n = 11..14

Раз исчерпывающий перебор при n >= 13 недоступен по бюджету (n=13:
13! * 13^3 ~ 1.5e11 базовых операций, по измеренной скорости — оценка порядка
нескольких часов на n=13 и на порядок больше на n=14), запущен направленный
локальный поиск, МАКСИМИЗИРУЮЩИЙ I(pi) (не случайная выборка): имитация
отжига (`experiments/h13i_toric_search.py`) со стартом из случайных
перестановок и из отражений с 0–3 случайными транспозициями, целевая функция
— сам I(pi) (прямое определение, независимая от быстрого алгоритма
реализация). Измерено: 1 вызов I(pi) — 0.5 мс (n=11), 0.7 мс (n=12), 0.9 мс
(n=13), 1.1 мс (n=14), 1.4 мс (n=15) — используется для оценки бюджета по
правилу 10.

Результаты (полный прогон, см. `data/runs/h13i_verdict/report.md` для точных
команд, seed, числа испытаний и времени): при n = 11, 12, 14 лучший найденный
I(pi) СОВПАДАЕТ с `floor((n−1)^2/4)` (25, 30, 42) и всегда достигается на
отражении или его повороте; при n = 13 лучший найденный при том же бюджете
итераций — 36 = floor(12^2/4) (максимум, тот же, что и цель) при более
длинных прогонах, и не выше цели при коротких. Ни на одном из 4 <= n <= 14 не
найдено pi с `I(pi) > floor((n−1)^2/4)`. Это SAMPLED-подтверждение (не
исчерпывающее при n >= 13), но целенаправленное (отжиг максимизирует именно
I, а не проверяет случайные входы), и оно согласуется с исчерпывающими
данными при n <= 12.

### 7.5. Вердикт и следующий шаг

H13-I остаётся CONJECTURED при всех n >= 4; VERIFIED расширена с
`4 <= n <= 10` до `4 <= n <= 12` (исчерпывающе); SAMPLED (целевой поиск) —
без контрпримера при `4 <= n <= 14`. Новое понимание (лемма D) объясняет
структурно, почему прямые усредняющие и «диагональные» сокращения перебора
не работают: слагаемое `V_{ij}` (собственные инверсии pi) не является
разрезом ни по какому порогу, и любая попытка «угадать» разрез без полного
перебора должна либо переоткрыть I(pi) целиком (как «диагональное»
ограничение), либо унаследовать дефект локальной индукции по элементу
(как гибридное усреднение). Ни один из способов не даёт общего доказательства
за бюджет сессии; контрпример также не найден.

Следующий шаг (per PLAN §8 и это задание): переход к индукции H3 (PLAN §4.3,
п. 3). Конкретно: определить вложение задачи размера n−1 в задачу размера n
для ИСХОДНОЙ цели `D_n <= B_n` (не для I(pi) отдельно — редукция по одному
элементу для самого H13-I уже отвергнута, п. 1.7); нужно явно построить, как
слово для `sigma_{n-1}` (или произвольной pi размера n−1) вкладывается в
слово для перестановки размера n, полученной естественным расширением
(например, добавлением элемента n−1 на новую позицию с последующей
корректировкой), посчитать точную стоимость вставки (число дополнительных
обменов и вращений головы) и подтвердить или опровергнуть рекуррентность
`T_n <= T_{n-1} + (n-1)` (H3), которая при `T_4 <= 6` в точности даёт `B_n` с
нулевым запасом. Ключевая трудность, отмеченная в PLAN §4.3 п. 3: L и R
вращают весь массив, поэтому «зафиксировать» один элемент при вложении
меньшей задачи нельзя молча — нужно явно учесть, что база (n−1)-конструкции
читается на окружности размера n, и посчитать стоимость восстановления
ориентации/головы после вставки. Бюджет — 2 сессии до вердикта (как и
намечено в PLAN §4.3 изначально).
