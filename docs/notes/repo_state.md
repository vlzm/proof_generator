# Состояние репозитория — что готово и что делать дальше

Обновляется в конце каждой сессии. Быстрый вход для новой сессии:
прочитать AGENTS.md (правила), затем этот файл, затем PLAN §8.

## Готово

Сессия 1 (12.09.2026):

- Ядро oracle-1.0: `oracle/moves.py` + тесты, `verify_word.py`,
  независимый сертификатор `verify_distances.py`.
- BFS: эталонный `exact/bfs.py` и ускоренный `exact/bfs_fast.c`
  (побайтово совпадают на 4<=n<=11).
- Таблицы расстояний сертифицированы для 4 <= n <= 12; n=11, 12 (.bin)
  не в git из-за размера — sha256 в `data/tables/dist_n1{1,2}.json`,
  воспроизведение: `gcc -O2 -o exact/bfs_fast exact/bfs_fast.c &&
  ./exact/bfs_fast 11 data/tables/dist_n11.bin && ./exact/bfs_fast 12
  data/tables/dist_n12.bin` (~4 мин на n=12), сверить sha256.
- VERIFIED при 4 <= n <= 12: D_n = B_n; единственность sigma_n; слой
  D-1 = 3 соседа sigma_n; d(rev_n) = D_n - 2; расщепление всех геодезических
  (floor((n-1)^2/4), floor(n^2/4)). Реестр: CLAIMS.md, строки C2v–C14v.
- `bounds/known.py`: все заявленные формулы в точной арифметике.

Сессия 2 (12.09.2026):

- Входные файлы добавлены владельцем (коммит 972890f): N1
  `LRX_STRICT_PROOF_RU-1.md`, `lrx_2n3_proof_checked.md`, `lrx_2n3_check.py`,
  `lrx_gap_improvement.md`. `verify_lrx.py`/`verification.json` из N1 §12
  отсутствуют.
- Аудит N1 завершён: `docs/notes/strict_proof_audit.md` — пробелов нет,
  C16–C21 PROVED (локальный аудит одной сессии). Первая локально
  подтверждённая общая верхняя оценка: `D_n <= U_n` (`certified_bound`).
- `checks/check_C16.py` — независимый чекер по тексту N1 (PASS, 77 с).
  Отчёт `data/runs/check_C16/`.
- Строки реестра C16v, C18v, C25 (конструкция N1 при лучшем c даёт
  ровно B_n на sigma_n; VERIFIED 4<=n<=12), гипотеза H8.

Сессия 3 (12.09.2026) — карта потерь N1 (PLAN §4.1):

- `constructions/strict_upper.py` (strict_upper-1.0): конструкция N1 как
  модуль (`shift_word`, `sorting_word`), слова совпадают с чекером на всех
  pi при 4<=n<=7; вариант маршрута по переносчикам (`carrier_route`) помечен
  как вариант (правило 16).
- `experiments/loss_map.py` (loss_map-1.1) и отчёт
  `data/runs/strict_loss_audit/report.md`: все pi при 4<=n<=9, семейства
  4<=n<=12, выборки n = 20, 50, 100. Раздельные потери по пунктам 1–5 §4.1,
  зазоры цепочки s, худшие входы с таблицами правила 15.
- Итог: зазор `U_n − B_n ≈ 1.172n` целиком в доказательстве: проводка §§5–9
  (≈ 0.92n на sigma_n) и усреднение H(c) по c в (3.4) (n/4 + O(1)); слова при
  лучшем c не длиннее B_n + 2 при 4<=n<=8, B_n + 3 при n = 9 (растёт: H9
  REFUTED, C26); с маршрутом по переносчикам — не длиннее B_n + 1, после
  сокращения B_n при 4<=n<=9 (H10, C27/C27v). Направление §4.2 выбрано: H10.

Сессия 4 (13.09.2026) — H10 на отражениях (PLAN §8, шаг 1):

- `docs/proofs/h10_reflections.md`: доказана теорема 1 — точная формула
  `min_c [2F_c − S_c + H(c)] − B_n` для всех отражений `pi(i) = h − i`
  при всех n >= 4 (0; −1 при n ≡ 0 mod 4 и чётном h; +1 при n ≡ 2 mod 4
  и чётном h). Следствия: H10 (C27) PROVED на отражениях; `d(pi) <= B_n + 1`
  для любого отражения; предложение 1 — конструкция N1 на sigma_n даёт ровно
  B_n с расщеплением `(floor((n−1)²/4), floor(n²/4))` при c из {0,2,n−2}
  (n ≡ 0 mod 4) или {1,n−1} (иначе). Отсюда C25 и C9 — PROVED (раньше
  CONJECTURED/CLAIMED), H8 закрыта.
- Новые леммы реестра: C29 (`S(C) >= 3`), C30 (побуквенный подсчёт
  `N_X`, `N_rot`), C31 (достаточное условие общего случая; покрытие
  100/91.7/92.5/90.3/83.1 % при n = 4..8 — падает, одного его мало).
- `checks/check_C28.py` (check_C28-1.0): режимы `--reflections` (все h, c при
  4<=n<=40), `--crosscheck` (сверка с реальными словами), `--census`,
  `--exhaustive` (checkpoint, возобновляемый). Отчёт
  `data/runs/h10_reflections/report.md`.
- Общий случай H10 не доказан. Препятствие измерено: семейство равенства
  (запас 0–1) не сводится к отражениям и не убывает с n.

Сессия 5 (13.09.2026) — H10 общий случай, вердикт (бюджет исчерпан):

- Перед началом сессии обнаружены 7 незамерженных параллельных веток
  (`claude/peaceful-hopper-{clbtmn,d8qzkv,fo34gf,q3x945,r35ukv,tu03kt,yfqkde}`),
  независимо и без ведома друг друга выполнившие шаг 1 плана (H10 на
  отражениях) заново: правило проекта «не открывать PR без явной просьбы»
  привело к тому, что ни один результат не попадал в слитую историю, и
  каждая новая автономная сессия видела ту же «следующую задачу». Лучший
  результат (`d8qzkv`, commit fe396e3 — C28-C31, доказательство H8/C9)
  перенесён в эту ветку `git cherry-pick`, воспроизведён повторным запуском
  `check_C28.py`. Систематическая причина требует решения владельца
  (мержить ветку, включить обязательные PR для этой автономной задачи, или
  явно синхронизировать через docs/notes перед стартом), не только записи
  в этом файле.
- Доказана лемма C32: `R_c <= 2(n − g_max)`, где `g_max` — наибольший
  промежуток между соседними точками `carriers(f_c) u {0,c}`
  (`docs/proofs/carrier_route_gap.md`); не зависит от структуры pi.
  Усиленное достаточное условие H10 (SC') на её основе покрывает больше
  входов, чем прежнее (SC, C31): 100/100/95.3/92.8/90.8/89.7 % против
  100/91.7/92.5/90.3/83.1/81.5 % при n = 4..9 (`checks/check_C32.py
  --exhaustive 9`, отчёт `data/runs/check_C32/`), но не достигает 100 % и
  не убывает к нулю.
- Консолидированный вердикт `docs/notes/H10_general_obstruction.md`
  (объединяет материал этой сессии и двух параллельных веток `yfqkde`,
  `q3x945`): структурная причина разрыва — F-минимизирующий сдвиг c и
  g_max-максимизирующий (или R_c-минимизирующий) сдвиг c в общем случае
  разные; связность носителя нагрузки (N1 §1) ограничивает разброс
  переносчиков только внутри одного цикла f_c, не между разными циклами.
- **H10 (C27) — CONJECTURED в общем случае**, бюджет 2 сессии (сессии 4-5)
  исчерпан по правилу 12 AGENTS.md. Не продолжается без новой постановки
  бюджета владельцем.

Сессия 6 (13.09.2026) — аудит C1 (прежний комплект), S1 заблокирован:

- Владелец ответил «продолжай по плану» без выбора одного из трёх
  вариантов сессии 5. Попытка S1 (C8) заблокирована средой: исходящий
  доступ к arxiv.org запрещён политикой egress-прокси (`EGRESS_BLOCKED`,
  и через `curl`, и через `WebFetch`) — не временная ошибка, требует
  решения владельца (доступ к домену, либо файл с текстом источника вне
  `docs/incoming/`, которая остаётся «только чтение» для уже переданных
  файлов). Переключение на неблокированную часть того же варианта — C1.
- Аудит `docs/incoming/lrx_2n3_proof_checked.md` §§1-6
  (`docs/notes/C1_audit.md`): пробелов не найдено. Локальная конструкция
  §§1-2 совпадает с уже проверенной N1 §§1-3 (C18); специфика C1 —
  неравенство (5) `3nS(C)>=5(F(C)+k)` и усреднение §§4-5 до `A_n`.
- `checks/check_C1.py` (check_C1-1.0): (5) проверена на 6 590 710
  экземплярах цикла (4<=n<=9, все pi, все c) — 0 нарушений; округление
  точной арифметикой при 4<=n<=100000 — 0 расхождений с формулой
  источника; семейство резкости §6 (r=2..25) — точное равенство; кросс-
  исполнение 3 628 776 слов источника независимым `oracle/moves.py` —
  0 отказов. Отчёт `data/runs/check_C1/`.
- **C1 — PROVED** (CLAIMS.md). Слабее C16/U_n при каждом n; не влияет
  на C15.

## Чего нет

- Аудит S1 (C8) заблокирован в этой среде (см. сессию 6, egress-прокси);
  аудит gap-файла (C4/C10/C11) не выполнен, `checks/check_gap_T1.py`
  не создан. C1 — аудирована (сессия 6).
- Модули §9.7 (invariants), §9.8 (evaluation — общий оценщик; есть только
  интерфейс `sorting_word` в `strict_upper.py`) не созданы.
- C22 (H6) без чекера; статистики для него считает `check_C16.py`.
- H10 (C27) в общем случае не доказана; бюджет исчерпан (см. сессию 5) —
  доказана на отражениях (C28) и на pi, удовлетворяющих (SC)/(SC')
  (C31/C32) хотя бы при одном c (большинство, не все входы при n <= 9).
- Вторая часть C27 (после свободного сокращения `<= B_n`) проверена лишь
  при 4 <= n <= 9: при n = 10 слова не строились.
- 6 из 7 обнаруженных в сессии 5 параллельных веток (все, кроме
  `d8qzkv`, уже перенесённой) остаются незамерженными; их числовой материал
  (не хуже уже перенесённого) не переносился — не требовалось после
  консолидации, но сами ветки не удалены и не закрыты.

## Следующая задача

Не задана владельцем. Варианты без приоритета:

1. Новый бюджет на H10 общий случай — начать с нерассмотренного в сессии 5
   инволютивного случая (`H10_reflections.md` §5, `H10_general_obstruction.md`
   §6) или направленного поиска контрпримеров среди блочных перестановок.
2. Аудит gap-файла (C4/C10/C11, `docs/incoming/lrx_gap_improvement.md`).
   S1 (C8) — тот же вариант, но заблокирован средой (нужно решение
   владельца, см. выше).
3. Решить судьбу дублирующих веток сессии 5 (список выше) и, при желании
   владельца, изменить процесс (PR по умолчанию для автономных сессий),
   чтобы устранить причину дублирования.
4. Модули §9.7 (invariants), §9.8 (общий оценщик `evaluation`).
