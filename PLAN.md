# PLAN.md — гипотезы, фазы, бюджеты, спецификации

Живой файл. В конце каждой сессии обновляются статусы гипотез (§0),
состояние фаз (§1) и следующий эксперимент (§8).

Бюджеты ниже — ориентировочные; единица — сессия агента, долгие прогоны
считаются отдельно в часах процессорного времени. Числа правит пользователь.

---

## 0. Рабочие гипотезы

Гипотезам разрешено быть неверными. Опровергнутая не удаляется: помечается
REFUTED со ссылкой на контрпример. Основание и статус — обязательны.

| ID | Гипотеза | Основание | Статус |
|---|---|---|---|
| H1 | При всех n >= 4 минимум N_X по геодезическим до sigma_n равен floor((n-1)^2/4), и тогда N_rot = floor(n^2/4) | BFS 4 <= n <= 8 (C13) | наблюдение |
| H2 | Самый далёкий класс окружности — обойдённая в обратную сторону; слой D_n - 1 — три соседа sigma_n | BFS 4 <= n <= 8 (C14) | наблюдение |
| H3 | Индукция: переход от sigma_(n-1) к sigma_n стоит floor((n-1)/2) обменов и floor(n/2) шагов головы; рекуррента T_n <= T_(n-1) + (n-1) при T_4 <= 6 даёт ровно B_n, запас ноль | n - 1 = floor(n/2) + floor((n-1)/2), согласуется с H1 | идея |
| H4 | Базовая конструкция C1 делает порядка p = floor(n^2/4) обменов; на sigma_n это около floor(n/2) лишних обменов | Историческая эвристика по слагаемому 2p в A_n, которое само не определяет N_X | идея, не подтверждена; сохранить и проверить прямым подсчётом по словам |
| H5 | Если S1 доказывает нижнюю оценку допустимым потенциалом Phi с Phi(sigma_n) = B_n, то Phi измеряет потери любой конструкции по ходам: ход потрачен впустую, если Phi не упал на 1 | Определение допустимости (PROBLEM §7) | идея; зависит от метода S1 |
| H6 | `2mu + overline(E_c) >= n-1` для всех n >= 4 и pi из S_n | N1 §10.2, C22; условный путь к `D_n <= B_n+n` | CONJECTURED; не входит в доказательство C16 |
| H7 | Выбор сдвига, фактическое позиционирование и сокращения позволяют существенно усилить усреднённую оценку N1 | C24 показывает остаток порядка n/4 у выражения (3.4) даже с точным s на отражениях; `check_C16.py`: при лучшем c фактическая длина на sigma_n равна B_n (4<=n<=101), max по всем pi при n<=7 не выше B_n+2 | измерено (сессия 3, `data/runs/strict_loss_audit/report.md`): при лучшем c слова не длиннее B_n + 2 на всех pi при 4<=n<=8; зазор U_n − B_n на sigma_n = проводка §§5–9 (≈ 0.92n) + усреднение H(c) по c (n/4 + O(1)) |
| H8 | Для sigma_n лучший сдвиг конструкции N1 даёт слово длины ровно B_n при всех n >= 4 (c из {0, 2, n-2} при n = 0 mod 4, из {1, n-1} иначе); общая формула длины доказуема прямым подсчётом по транспозициям f_c | C25: VERIFIED 4<=n<=12, слова при n = 20, 50, 100, 101 | гипотеза; даёт локальное доказательство C9 |
| H9 | `min_c [2F_c - S_c + H(c)] <= B_n + 2` при всех n >= 4, pi из S_n (минимум по c оценки N1 (3.3) без усреднения) | Карта потерь: VERIFIED 4<=n<=8 (все pi), SAMPLED n = 20 (<= B_n + 1), 50, 100 (<= B_n); на отражениях равенство B_n или B_n + 1 (n = 2 mod 4, чётное h); F_c = sum_i delta_n(i - pi(i), c) — 1-медиана | CONJECTURED (C26); выбранное направление §4.2 |
| H10 | Вариант с маршрутом только по переносчикам: `min_c [2F_c - S_c + R_c] <= B_n + 1`, после свободного сокращения `<= B_n`, при всех n >= 4 | Карта потерь: VERIFIED 4<=n<=8 (+1 только на отражении n = 6, h = 2), семейства до n = 12, SAMPLED n = 20, 50, 100; леммы N1 на вариант не перенесены (правило 16) | CONJECTURED (C27); рабочий вариант направления §4.2 |

---

## 1. Фазы и бюджеты

Состояние этой редакции: прочитаны четыре управляющих документа и N1;
выполнено предварительное ревью, добавлен алгебраический вывод C24.
Полный аудит N1, воспроизведение §12 и инвентаризация репозитория не выполнены.
«Не начата» ниже означает отсутствие подтверждённого выполнения в доступном
комплекте; при доступе к репозиторию состояние уточняется по артефактам.

| Фаза | Цель | Бюджет | Критерий завершения | Состояние |
|---|---|---|---|---|
| 0 | Проверить существующее ядро и таблицы 4..8; заполнить нужные пробелы | До 1 сессии на недостающее | Подтверждённые ядро и покрытие, конечные C2/C5/C6/C13/C14 со ссылками на сертификаты | состояние репозитория уточнить; не обязательный блокер текстового аудита |
| 1 | Приоритетный аудит N1; отдельно S1 и прежних оценок | 1 сессия на N1 (израсходована); 2 на S1; до 1 на прежние файлы при необходимости | Обоснованные статусы C16–C21 и C8, карта пробелов; разделены текстовый аудит и воспроизведение §12 | аудит N1 выполнен 12.09.2026: C16–C21 PROVED (локально), `strict_proof_audit.md`, `check_C16.py` PASS; S1 и прежние файлы (C1, gap) не аудированы |
| 2 | Конечная цель C15; промежуточные улучшения фиксируются отдельно | 1 сессия на карту потерь N1; затем 2 на выбранное направление до вердикта | C15 только при доказанной потере <= 0; для промежуточного результата — точная формула и статус либо вердикт | карта потерь §4.1 построена (сессия 3, `data/runs/strict_loss_audit/`); направление §4.2 выбрано: прямая оценка минимума по c (H9/H10), бюджет 2 сессии |
| 3 | Нижняя оценка | Неактивна, пока аудит C8 не выявил пробел; при активации 2 сессии на потенциалы, 1 на формулы по таблицам | Лемма или семейство оценок; либо вердикт | неактивна |
| 4 | Таблицы n >= 9 | Только под конкретный вопрос; n = 9..10 до 1 часа ЦП; n = 11 после измерения и с checkpoint | Ответ на поставленный вопрос | по требованию |
| 5 | Аудит следующего нового результата | 1 сессия на результат | Отчёт аудита, обновлённые статусы | по требованию |

Бюджеты — пределы до письменного вердикта, а не обязательство закончить
доказательство за сессию. Полные прогоны из N1 §12 сначала оцениваются
по скорости и памяти; исходная команда не является разрешением на долгий запуск.

---

## 2. Фаза 0. Ядро и таблицы

1. Проверить, существует ли репозиторий и что подтверждённо сделано;
   продолжать от подтверждённого состояния.
2. Проверить доступность N1, `verify_lrx.py`, `verification.json`, прежних
   входных файлов и S1/S2; перечислить недостающее и зафиксировать ограничения.
   Отсутствие старых источников не блокирует самодостаточный текст N1.
3. При отсутствии ядра создать необходимые модули (§9.1–9.4):
   `oracle/moves.py` с тестами, `oracle/verify_word.py`, простой BFS,
   `oracle/verify_distances.py`; карту каталогов и CLAUDE.md — по AGENTS.md.
4. Проверить существующие таблицы n = 4..8; построить недостающие,
   сертифицировать и сохранить с метаданными. Подтверждённые таблицы не
   пересчитывать только из-за появления N1.
5. Использовать подтверждённые отчёты `mine_worst.py` и `mine_geodesics.py`
   для n = 4..8 (§9.5–9.6), выполнить недостающие проверки.
6. Зафиксировать версию ядра; обновить CLAIMS.md (конечные версии VERIFIED
   отдельными строками, общие кванторы остаются CONJECTURED); запись в LOGS.md.

Не требуется для завершения фазы: BFS n >= 9, большой оценщик конструкций,
поиск формулы d(p). Фаза 0 обслуживает текущий вопрос: текстовый аудит N1
и проверка действия отдельного слова не требуют полного BFS.

---

## 3. Фаза 1. Аудит и выбор фронта

### 3.1. N1 — ближайшая задача

Результат аудита — `docs/notes/strict_proof_audit.md`; этот путь задаёт
планируемый артефакт, а не уже существующий отчёт. Бюджет — 1 сессия.

1. Зафиксировать версию/контрольную сумму N1 и наличие кода/отчёта из §12.
   Сохранить исходник без правок в `docs/incoming/`; рабочую адаптацию
   создавать отдельно только при необходимости.
2. Проверить C18: правило нагрузок, допустимый переносчик, стягивание,
   действие слова в текущих координатах, непересекающиеся сокращения,
   сшивку циклов и все вращения. Сверить конвенции с примером XL.
3. Проверить тождество N1 §4 и C19: антиподальную поправку, min(e_i) = 0,
   исключительные пары, выбор общего партнёра и суммирование бюджета Q.
4. Проверить C20 для обеих чётностей; для C21 — связность положительного
   носителя и все случаи малых нагрузок, включая антиподальную транспозицию.
5. Проверить исключение параметров (§8), действительный радикал и округления
   C16/C17 (§9); n = 5 и чётные/нечётные диапазоны рассмотреть явно.
6. Отделить C22/C23 и упоминания (17?) от цепочки C16. Проверить вывод C24
   как ограничение выражения (3.4), а не нижнюю оценку алгоритма.
7. Для вычислительных проверок использовать доступный `verify_lrx.py` после
   чтения; сопоставить его с N1. При отсутствии кода написать независимые
   проверки по тексту, явно назвав их новыми, а не восстановленным оригиналом.
   Сначала малые диагностические случаи, полный диапазон §12 — после оценки
   бюджета. Отсутствие этих артефактов не блокирует логический аудит §§1–9.
8. Исход: обоснованные статусы C16–C21, конкретные пробелы при наличии,
   покрытие собственных проверок и карта потерь для фазы 2. Сообщённый PASS
   отдельно от фактически выполненных прогонов.

### 3.2. Нижняя оценка S1 и прежние верхние оценки

Аудит S1 — отдельная сессия со своим документом `docs/notes/literature_audit.md`:

1. Перевести постановку S1 в проектные конвенции: генераторы (12), (12…n),
   (1n…2) в 1-индексной циклической записи — какие из них L, R, X, с какой
   стороны действие, в каком порядке произведение. Сверить на примере XL.
2. Выделить главные леммы нижней оценки и составить план аудита.
3. Проверить общие переходы, область действия, малые случаи. Зелёные примеры
   не удостоверяют минимальность произвольного разложения для общего n.
4. Записать отдельно: доказывает ли аргумент нижнюю оценку именно для sigma_n.
5. Если доказательство использует потенциал — зарегистрировать его
   в invariants/ и подключить к оценщику как измеритель потерь (H5).
6. Исход: C8 -> PROVED (локальный аудит) либо точный неустановленный переход.

При наличии входных файлов воспроизвести C1 и проверить C4/C10/C11 (§9.9),
если это нужно для подтверждённой границы или сравнения конструкций.
Старые результаты сохраняются; сравниваются точные формулы, а не только
асимптотические коэффициенты. H4 проверяется подсчётом по словам.

Выбор фронта. Нижняя оценка принята — весь ресурс на фазу 2, фаза 3 остаётся
неактивной. Найден конкретный пробел — работа над ним становится отдельной
задачей с бюджетом; широкий поиск потенциалов с нуля не начинается автоматически.

---

## 4. Фаза 2. Верхняя оценка

Конечная цель — C15: `D_n <= B_n`. Новая C16 уменьшает линейный коэффициент
до kappa ≈ 1.17225. Улучшение коэффициента, границы с остатком o(n), O(1)
и точная B_n — разные достижения. Каждое регистрируется с собственной
формулой; остаток O(1) сам по себе не завершает C15.

### 4.1. Первый эксперимент: карта потерь N1

Бюджет — 1 сессия после аудита нужных частей конструкции. Использовать
подтверждённое ядро и таблицы; при отсутствии полного BFS можно проверить
слова и внутренние неравенства, но нельзя выдавать `len - B_n` за `len - d(pi)`.

Реализовать точно описанную конструкцию как `constructions/strict_upper.py`
с выбором сдвига по явно заданному правилу. Для каждого pi и каждого c
сохранять реальные слова и статистики из правила 15 AGENTS.md. Разделять:

1. Локальные слова: их фактическая суммарная длина против `2F_c-S_c`.
2. Позиционирование: фактический внешний маршрут против H(c).
3. Выбор сдвига: минимум фактической длины, минимум `2F_c-S_c+H(c)`
   и среднее этого выражения — три разные величины.
4. Оценки s: точное s против правых частей (7.3), (8.1), (8.2), (8.3)
   N1; для каждого перехода — разница и условия равенства.
5. Стыки: длина полного слова до и после дополнительного свободного
   сокращения; N_X и N_rot в обоих случаях. Сокращение не подменяет
   длину того варианта слова, который оценивается на конкретном шаге.

Начать с отражений (включая sigma_n и все её вращения), малых возмущений
отражений и семейства C23; затем все перестановки 4 <= n <= 7, расширение
до n = 8 по бюджету. C24 служит точным контрольным расчётом: на отражениях
даже точное s оставляет в (3.4) остаток порядка n/4. Это направляет поиск
на переход к среднему, стоимость H(c) и сами слова, а не только на §§5–8.

Выход — отчёт `data/runs/strict_loss_audit/` с покрытием, раздельными потерями,
трудными входами и одним выбранным следующим направлением.

Выполнено 12.09.2026 (сессия 3): `constructions/strict_upper.py`,
`experiments/loss_map.py`, отчёт `data/runs/strict_loss_audit/report.md`
(все pi при 4<=n<=9, семейства до n = 12, выборки n = 20, 50, 100). Итог:
потери п. 1 (локальные слова) и п. 5 (стыки) — O(1); п. 2 (маршрут) — до n,
в среднем 2.5 при n = 8; п. 3 (среднее против минимума) — n/4 + O(1) на
отражениях за счёт усреднения H(c), до 0.6n на входах с малым d за счёт F;
п. 4 (цепочка s) — ≈ 0.92n на sigma_n, ≈ 0.75n на случайных входах. Слова при
лучшем c не длиннее B_n + 2 (4<=n<=8). Выбрано направление: прямая оценка
минимума по c (H9/H10).

### 4.2. Направления после диагностики

- Улучшать доказательство той же конструкции: корреляции F_c, S_c и H(c),
  выигрыш от выбора c, невозможность одновременного достижения всех худших
  случаев, точная стоимость посещения только нужных переносчиков.
- Улучшать слова: порядок циклов, выбор переносчика, маршруты и сокращения
  на стыках, согласованные варианты дуг. Некратчайшие дуги и другие правила
  антиподов требуют повторного обоснования затронутых лемм.
- Проверить H6/C22 как отдельную гипотезу с возможным промежуточным результатом
  `D_n <= B_n+n`. Не включать её в C16 и не считать этапом, обязательным для C15.
- Сравнить с прежним C1 и алгоритмом S2 на одном наборе входов, если доступны
  реализации. Лучший наблюдаемый результат не является общей оценкой.

На каждое выбранное направление — 2 сессии до письменного вердикта.
Усиление оценки без изменения слов — полноценный математический результат;
изменение алгоритма не требуется само по себе.

### 4.3. Конструкции для sigma_n и индукция

Это сохраняемые альтернативные направления, а не обязательные шаги перед
исследованием N1:

1. Явное семейство слов длины B_n для sigma_n по геодезическим
   (PROBLEM §4.2), сверка с S2/C9; 1–2 сессии при выборе направления.
2. Обобщение на все отражения с разными положениями головы, затем на
   произвольную pi. Конструкция только для sigma_n не доказывает C15.
3. Индуктивная редукция H3: определить вложение меньшей задачи и стоимость
   имитации её ходов. Фиксированный элемент нельзя молча исключать: L и R
   вращают весь массив. Учесть восстановление ориентации; запас рекурренты
   нулевой. Бюджет — 2 сессии до вердикта.

Цикл работы: вопрос -> кандидат -> оценка -> трудные входы -> разбор причины
-> гипотеза. Хранить полезные варианты по классам входов. Для портфеля
алгоритмов определить правило выбора и его сложность. На больших n искать
входы, увеличивающие длину или запас над целью: худшие входы алгоритма
не обязаны быть самыми далёкими вершинами графа.

---

## 5. Фаза 3. Нижняя оценка — при конкретной необходимости

- Потенциалы. Калибровать framework на известных слабых и недопустимых
  функциях; отделять нарушение допустимости от недостижения цели; собирать
  оценки из слабых допустимых частей через max (PROBLEM §7). Не требовать
  точности Phi на всех перестановках, если цель — sigma_n.
- Ограничения на произвольное слово. Отправная точка — модель PROBLEM §2:
  совместные ограничения на число обменов, пробег головы, конечную ориентацию
  и порядок пересечений элементов. Нельзя оценивать только путь с минимальным
  N_X: лишние обмены могут экономить вращения. Аргумент охватывает все
  допустимые слова или имеет доказанную редукцию к рассматриваемому классу.
- Формулы по малым таблицам: признаки и простые формулы с бюджетом 1 сессия.
  Цель — лемма или семейство оценок; хорошая регрессия точной d нижней оценки
  не даёт.

---

## 6. Фаза 4. Таблицы n >= 9

Строятся, если отвечают на конкретный вопрос: контрпример, проверка семейства,
структура почти максимального слоя, качество конструкции. Перед запуском —
правило 10 из AGENTS.md. Максимум частичной таблицы диаметром не является.

---

## 7. Фаза 5. Аудит нового результата

1. Установка «найти ошибку»: переходы, зависимости, область n, граничные
   случаи, условия равенства.
2. Для конструкций проверить: ничьи направлений, антиподальные точки, пустые
   и единичные наборы используемых объектов, циклы через позицию 0, чётные
   и нечётные n, случаи около численных порогов.
3. Независимые проверки писать по тексту, по возможности до чтения кода
   автора; отдельная реализация ходов допустима.
4. При ошибке обновить затронутые утверждения и зависимые доказательства;
   сохранить старые результаты и причину пересмотра.

---

## 8. Следующая задача и состояние редакции

Выполнено 12.09.2026 (сессия 1 в репозитории): фаза 0 закрыта и перекрыта.
Создано ядро oracle-1.0 с тестами, два BFS (эталонный Python и ускоренный C,
побайтово совпадающие на 4<=n<=10), независимый сертификатор таблиц
с самопроверкой на повреждениях. Таблицы построены и сертифицированы
для 4 <= n <= 12; все опорные данные PROBLEM §4.1–4.2 воспроизведены
и расширены: D_n = B_n, единственность sigma_n, слой D-1 = три соседа,
d(rev_n) = D_n - 2, расщепление геодезических (floor((n-1)^2/4), floor(n^2/4))
подтверждены при 4 <= n <= 12. Подробности — LOGS.md,
`data/runs/session1_report.md`; статусы — CLAIMS.md (C2v, C5v, C6v, C13v, C14v).

Прежний текст состояния (до репозитория): управляющие документы согласованы
с N1; зарегистрированы C16–C24; полный аудит N1 и его заявленные прогоны
не выполнены.

Выполнено 12.09.2026 (сессия 2): §3.1 закрыт. N1 добавлен владельцем,
аудит §§1–9 не нашёл пробелов; C16–C21 PROVED (локальный аудит);
`checks/check_C16.py` воспроизвёл диапазоны N1 §12 собственным кодом и
дополнительно построил слова при n = 50, 100, 101. Артефакты:
`docs/notes/strict_proof_audit.md`, `data/runs/check_C16/`. Код `verify_lrx.py`
из N1 §12 в репозитории отсутствует и не требуется.

Следующая задача — карта потерь N1 (§4.1), бюджет 1 сессия. Стартовые данные
уже есть (аудит §5): при лучшем c конструкция даёт ровно B_n на sigma_n
(C25/H8), худшая фактическая длина по всем pi при n <= 7 — не выше B_n + 2,
тогда как доказанная оценка (3.4) теряет n/4 на переходе к среднему и на
H(c). Вопрос сессии: разложить потерю доказательства по пунктам 1–5 §4.1
(локальные слова, маршрут против H(c), min против среднего, точное s против
(7.3)–(8.3), стыки) на отражениях и всех pi при 4 <= n <= 7, и выбрать одно
направление §4.2. Отдельная кандидатная задача — доказать H8 (формула длины
конструкции на sigma_n), что даст локальное C9.

Выполнено 12.09.2026 (сессия 3): карта потерь §4.1 построена
(`data/runs/strict_loss_audit/report.md`, `constructions/strict_upper.py`,
`experiments/loss_map.py`). Ответ: слова конструкции при лучшем c не длиннее
B_n + 2 на всех pi при 4<=n<=8 (и B_n на sigma_n при всех проверенных n);
зазор `U_n − B_n ≈ 1.172n` целиком в доказательстве — проводка §§5–9
(≈ 0.92n на sigma_n) и усреднение H(c) по c в (3.4) (n/4 + O(1)). Потери
самих слов (маршрут, стыки, композиция циклов) — малы и только на входах с
малым d. Направление §4.2 выбрано: прямая оценка минимума по c без (3.4)
и без цепочки s.

Следующая задача — H9/H10 (C26/C27), бюджет 2 сессии до письменного вердикта.
Шаги: (1) доказать `min_c [2F_c − S_c + H(c)] <= B_n + 1` на отражениях
явно (данные C24/C25: F_c постоянна по c, S_c по чётности c, H(0) = n,
H(1) = n − 1); (2) для произвольной pi: связать медианный сдвиг
`argmin_c F_c` (тождество `F_c = sum_i delta_n(i − pi(i), c)`) с S_c и H(c):
нужна нижняя оценка S_c при c около медианы или аргумент «нельзя одновременно
иметь F_c = P, малое S_c и большое H(c) при всех c»; (3) при неудаче —
письменный вердикт с найденным препятствием и худшими входами из
`perms.json`/`perms_n9.json`. Отдельно доступна проверка H9 при n = 10
(таблица есть; 3.6 млн перестановок, ~3 ч в текущем коде — только после
оценки скорости, правило 10).

Параллельно доступны: аудит S1 (§3.2, C8) и аудит прежнего комплекта
(C1, gap-файл) — входные файлы теперь в `docs/incoming/`; доказательство
H8/C25 (формула длины на sigma_n).

При работе уже в репозитории: записать выполненные действия в LOGS.md,
обновить этот раздел и состояния фаз по фактическим артефактам.

---

## 9. Спецификации модулей

### 9.1. `oracle/moves.py`

Функции: `apply_move(state, move)`, `apply_word(state, word)` (слева направо),
`freely_reduce(word)` (сокращает XX, LR, RL), `delta(a, b, n)`, `sigma(n)`.
Предусловие: state — перестановка 0..n-1, n >= 2; проверяется на внешней
границе, а не в каждом внутреннем вызове BFS.

Обязательные тесты: пример XL; обратимость L/R; `X^2 = id`; `L^n = id`;
сохранение множества элементов; эквивалентность свободно сокращённого слова
исходному. `oracle/verify_word.py` проверяет алфавит и результат слова.

### 9.2. BFS и таблицы

- n <= 8: BFS от id_n со словарём «кортеж -> расстояние»; n = 9 так же, если
  измеренная память подходит.
- n >= 10: сначала измерить скорость и память на меньшем n; при необходимости
  код Лемера (биекция перестановок и чисел 0..n!-1; проверить взаимную
  обратность и совпадение с простым BFS) и плотный массив расстояний.
- Один байт на расстояние: около 3.6 МБ при n = 10, 40 МБ при n = 11,
  479 МБ при n = 12 — только массив, без очереди. Тип данных содержит отдельное
  значение «не посещено» и защищён от переполнения без опоры на гипотезу
  о диаметре.
- Время чистого Python не обещать заранее; ускорение (NumPy, компиляция,
  другой backend) выбирать по измеренному узкому месту; векторизация
  не гарантирует экономию памяти.
- Файл `dist_n{n}.bin` сопровождают метаданные: n, генераторы и конвенции,
  индексация, тип данных, версия кода, полнота обхода, максимальное
  расстояние, контрольная сумма; частичная таблица помечается partial.
- Долгий обход восстанавливается из checkpoint: расстояния, фронт и служебное
  состояние. Одного массива посещённых вершин недостаточно.

### 9.3. Сертификат таблицы (`oracle/verify_distances.py`)

Для полной таблицы h на всех S_n независимый модуль проверяет:

1. Все значения — неотрицательные целые; единственный ноль — в id_n.
2. Для каждого ребра {p,q}: `|h(p) - h(q)| <= 1`.
3. Для каждой p != id_n есть сосед q с `h(q) = h(p) - 1`.

Плюс полнота множества перестановок и корректность индексации. Тогда
h(p) = d(p): условие 2 даёт h(p) <= d(p) вдоль любого пути от id_n, условие 3
строит путь длины h(p) к единственной вершине с нулём.

Контрольная сумма удостоверяет целостность файла, не точность. Выборочная
проверка условий — диагностика, не сертификат. Проверяющий модуль проверяется
также на намеренно повреждённых таблицах.

### 9.4. Проверки опорных данных

На сертифицированных таблицах: D_n из PROBLEM §4.1; единственность sigma_n;
`d(rev_n) = D_n - 2`; слой D_n - 1 — три соседа sigma_n; два равенства
из PROBLEM §4.3. Максимум вычисляется один раз, вне перебора; BFS не
пересчитывается отдельно для каждого теста.

### 9.5. `experiments/mine_worst.py`

- Читает n и сертифицированную таблицу.
- Находит D_n, все максимумы, слои D_n - 1 и D_n - 2, профиль числа вершин
  по расстояниям.
- Проверяет C5, C6, C14 отдельно от формулы диаметра; вычисляет цикловой тип,
  неподвижные точки, `F_c(p)` для всех c.
- Группирует данные под действием преобразований PROBLEM §4.3, не называя
  обращение автоморфизмом.
- Выдаёт JSON, сводку, запись в журнале.

### 9.6. `experiments/mine_geodesics.py`

- Для sigma_n и каждой вершины слоёв D_n - 1, D_n - 2 при n <= 8 (n = 9 при
  наличии таблицы): число геодезических; распределение (N_X, N_rot); сами слова
  (все при n <= 6, выборка при больших n); длины серий вращений между обменами
  и хвост; последовательность позиций обмена h_1..h_k на окружности
  в неподвижной системе позиций.
- Проверяет C13. Выдаёт JSON и сводку.
- Назначение: образцы для конструкции (фаза 2, шаг 2) и мишень для нижней оценки.

### 9.7. `invariants/framework.py`

- Интерфейс: `phi(p: tuple[int, ...]) -> number` с явной областью n
  и арифметикой; бесконечности и NaN — ошибка вычисления.
- Допустимость: нормировка и рёбра L и X; при проверке всех p этого достаточно
  для R (R = L^-1); при выборочной проверке — все три хода. Для VERIFIED —
  все состояния и нужные рёбра при данном n, точная или строго
  контролируемая арифметика.
- Сила: `Phi(sigma_n)`, `B_n - Phi(sigma_n)`; при наличии таблицы — максимум
  нарушения `Phi(p) <= d(p)`, доля точных совпадений, распределение
  зазора `d(p) - Phi(p)`. Недостижение B_n не означает недопустимость.
- Отчёт содержит отдельно coverage, arithmetic, admissibility, strength.
  Выборка не даёт сертификата допустимости; проверка `Phi <= d` без проверки
  рёбер допустимость не доказывает.
- Предпочтительны целые и рациональные числа; float с допуском — только
  для поиска, с пометкой NUMERICAL.

### 9.8. `evaluation/constructions.py`

- Кандидат предоставляет `sorting_word(state) -> list[Move]`.
- Кандидат выполняется в отдельном процессе с бюджетом ресурсов; берётся
  само слово, не заявленный балл. Родитель проверяет алфавит и сортировку
  эталонными ходами.
- Сохраняются исходная и свободно сокращённая длина, N_X и N_rot; для сравнения
  заранее фиксируется один режим; соответствие доказанному алгоритму сохраняется.
- Лестница проверок — правило 9 AGENTS.md (для дорогих конструкций n = 8
  по бюджету). Кандидаты сравниваются на одном наборе входов; сохраняются
  худшие входы, время, покрытие.
- Исходы: CORRECT_ON_TESTED_INPUTS, WRONG_WORD, ERROR, TIMEOUT, RESOURCE_LIMIT.
  Таймаут — превышение бюджета, не контрпример.
- Метрики. При n <= 9 с точной таблицей — основная диагностика: распределение
  `len - d(p)` по всем p и топ входов; где известно расщепление геодезических,
  потеря делится на лишние обмены и лишний пробег. При больших n —
  `max_len_observed - B_n`; наблюдаемый максимум не выдаётся за оценку
  на всём S_n. Сравнение с формулой — диагностика, не критерий корректности.
- Кандидат, претендующий на явную полиномиальную конструкцию, не запускает
  поиск по всему S_n и не читает таблицы во время работы. Проверка импортов —
  дисциплина, не доказательство сложности; сложность обосновывается отдельно.
  Полиномиальность — цель конструкции, а не условие любого доказательства.
- Для N1 сохранять статистики по сдвигам и раздельные потери (§4.1,
  AGENTS.md правило 15). Выбор c по минимуму оценки и по минимуму фактической
  длины — разные режимы; правило, все проверенные c и время выбора входят
  в описание алгоритма. Минимум по неполному набору нельзя назвать минимумом
  по всему Z_n.
- Изоляция: отдельный процесс, таймаут, проверка в родителе. Ничего сильнее
  до появления массовой эволюции произвольного кода.

### 9.9. Проверки входных доказательств

`checks/check_C1.py` воспроизводит базовую конструкцию после чтения входных
файлов; адаптация ходов сверяется с исходным поведением; общий статус требует
и логического аудита текста.

`checks/check_gap_T1.py` создаётся после чтения gap-файла. Термины M, S, k,
нагрузка и карман сначала определяются по источнику. План:

- допустимые циклы при n = 4..8, включая все направления при ничьих дуг;
- заявленные леммы `M <= 2 => S >= k + 1`, `S >= min(k + 1, 5 + (k mod 2))`,
  F-оценки, исключительные случаи k = 6, 7;
- точная арифметика малых n, округлений A_n и порогов;
- заявленная средняя экономия против фактической — ровно с той мерой усреднения
  и теми вариантами конструкции, которые заданы в тексте;
- общий аргумент для диапазонов, отсутствующих при малых n, и целевые примеры
  около порогов 138 и 852.

Границы перебора соответствуют кванторам лемм; примеры около большого порога
не проверяют все перестановки этих размеров. Улучшения на 2 и 3 хода —
отдельные строки реестра и отдельные проверки.

### 9.10. `bounds/known.py`

Сохранить `target_diameter(n)`, рациональную `A_n(n)`,
`baseline_bound_2n3(n) = floor(A_n)` и прежние gap-формулы с областями действия.
Добавить `strict_upper_bound(n) = U_n` (C16),
`strict_upper_bound_simple(n)` (C17) и сравнение применимых формул.
Имена функций не удостоверяют результат; статусы и зависимости — CLAIMS.md.

Для U_n использовать целочисленную/символьную арифметику или удостоверенные
интервалы. Например, при `z = 3n^2-4n+1-4P`,
`a = 36nP+25n^2-43n-12P+36`, `b = 7n`, `d = 18n` имеем
`R_n = (a-b*sqrt(z))/d`. Поэтому
`U_n = (a-ceil_sqrt(b^2*z)) // d`, где ceil_sqrt — точный целочисленный
квадратный корень с округлением вверх. Обосновать и проверить обработку
полных квадратов; float с допуском не используется для итогового floor.
Для floor(kappa*n) применим тот же приём к `(31n-7n*sqrt(2))/18`.

Сравнение возвращает формулу, диапазон и ID. Подтверждённый минимум использует
PROVED-границы и, при наличии, VERIFIED-данные только в их конечном диапазоне.
Минимум CLAIMED-формул показывается отдельно. Если подтверждённых оценок
в подключённом наборе нет, результат так и помечается, без повышения статуса.
Контрольные значения U_n: 9, 27, 211, 5065 при n = 4, 7, 20, 100.

### 9.10a. Проверки N1 (`checks/check_C16.py`)

После чтения N1 реализовать независимую проверку C18–C21 и округлений C16/C17
с явной привязкой каждого assert к лемме. Нагрузки можно считать перечислением
рёбер; слова проверять фактическим применением букв. Отдельно проверять
опорные точки с нулевой ошибкой, исключительных партнёров, антиподы,
связность носителя, чётность n и формулы на отражениях C24.

Для C22 — отдельный режим поиска контрпримеров, для C23 — проверка заданного
семейства. Не смешивать их с обязательными assert доказательства C16.
Общее доказательство C16 не заменяется проходом всех конечных assert.

Покрытие отчёта: циклы, все перестановки, выборочные перестановки, проверенные
сдвиги, построенные слова и скалярные n — отдельные поля. Диапазоны из N1 §12
не копируются в локальный отчёт без выполнения; особенно не заявлять проверку
слов при n = 50 и 100 по исходному сообщению. Существующий `verify_lrx.py`
сохраняется как источник, новая реализация не выдаётся за него.

### 9.11. Формат доказательства (`docs/proofs/<id>.md`)

1. Точное утверждение, область n, конвенции, нумерованные леммы, зависимости,
   полный аргумент, раздел «Машинная сверка» с границами применимости.
2. `checks/check_<id>.py`: вычислительно проверяемые части с привязкой к леммам
   и отчётами о покрытии.
3. Отчёт аудита логических переходов, особенно не удостоверяемых конечными
   примерами.

Код проверки следует тексту; оптимизированная версия — отдельный алгоритм,
пока не обоснована эквивалентность нужных свойств. Если лемма не имеет
содержательной конечной проверки, описывается способ логического аудита;
assert ради наличия чекера не создаётся. Если общий случай сводится
к конечному перебору, отдельно удостоверяются полнота редукции
и исчерпывающее вычисление. Формальная верификация возможна, не обязательна.
